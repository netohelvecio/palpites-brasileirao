# Plan 9.5 — `/escolher` Command + OpenRoundJob Wiring (Tied Featured Match Poll)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar comando `/escolher <n>` (DM admin) ao `WhatsAppInboundHandler`; ramificar `OpenRoundJob` pra mandar a enquete (uma vez) quando rodada está em `awaiting_pick`; fechar o ciclo end-to-end (sync detecta empate → poll vai pro grupo → admin escolhe → próximo cron abre).

**Architecture:** Inbound handler ganha branch `^/escolher\s+(\d+)$` antes do parser de palpite, com gate `user.isAdmin`. Job ganha branch antes do flip pending→open: se `awaiting_pick`, manda poll/fallback uma vez (idempotência via `poll_message_id` em qualquer candidato).

**Tech Stack:** Adonis 7, Baileys, Japa (functional), TypeScript ESM.

**Spec:** `docs/superpowers/specs/2026-05-07-tied-featured-match-poll-design.md`

**Depends on:** Plans 9.1, 9.2, 9.3, 9.4.

---

### Task 1: Adicionar `findCurrentAwaitingPickAcrossSeasons` no `RoundRepository`

**Files:**
- Modify: `apps/api/app/repositories/round_repository.ts`

- [ ] **Step 1: Adicionar o método**

No final da classe `RoundRepository` em `apps/api/app/repositories/round_repository.ts`:

```ts
  findCurrentAwaitingPickAcrossSeasons() {
    return Round.query()
      .where('status', RoundStatus.AWAITING_PICK)
      .whereHas('season', (s) => {
        s.where('is_active', true)
      })
      .orderBy('created_at', 'asc')
      .first()
  }
```

- [ ] **Step 2: Typecheck**

Run:
```bash
cd apps/api && pnpm typecheck
```
Expected: limpo.

- [ ] **Step 3: Commit**

```bash
git add apps/api/app/repositories/round_repository.ts
git commit -m "feat(repos): add findCurrentAwaitingPickAcrossSeasons to RoundRepository"
```

---

### Task 2: `/escolher` no `WhatsAppInboundHandler` (TDD functional)

**Files:**
- Create: `apps/api/tests/functional/whatsapp_inbound_escolher.spec.ts`
- Modify: `apps/api/app/services/whatsapp_inbound_handler.ts`

- [ ] **Step 1: Teste functional**

```ts
// apps/api/tests/functional/whatsapp_inbound_escolher.spec.ts
import { test } from '@japa/runner'
import testUtils from '@adonisjs/core/services/test_utils'
import app from '@adonisjs/core/services/app'
import { DateTime } from 'luxon'
import WhatsAppInboundHandler from '#services/whatsapp_inbound_handler'
import WhatsAppClient, {
  type IncomingMessageHandler,
  type PollSendResult,
  type WhatsAppMode,
} from '#integrations/whatsapp/whatsapp_client'
import { SeasonFactory } from '#factories/season_factory'
import { RoundFactory } from '#factories/round_factory'
import { RoundMatchCandidateFactory } from '#factories/round_match_candidate_factory'
import { UserFactory } from '#factories/user_factory'
import RoundRepository from '#repositories/round_repository'
import MatchRepository from '#repositories/match_repository'

class FakeClient extends WhatsAppClient {
  readonly mode: WhatsAppMode = 'stub'
  groupCalls: string[] = []
  userCalls: { to: string; text: string }[] = []
  async connect() {}
  async disconnect() {}
  isConnected() {
    return true
  }
  async sendToGroup(text: string) {
    this.groupCalls.push(text)
  }
  async sendToUser(to: string, text: string) {
    this.userCalls.push({ to, text })
  }
  async sendPollToGroup(_q: string, _o: string[]): Promise<PollSendResult> {
    return { messageId: 'noop' }
  }
  onMessage(_h: IncomingMessageHandler) {}
}

test.group('WhatsAppInboundHandler — /escolher', (group) => {
  group.each.setup(() => testUtils.db().wrapInGlobalTransaction())

  test('admin com posição válida → cria match + flipa pra pending + responde DM + posta no grupo', async ({
    assert,
  }) => {
    const fake = new FakeClient()
    app.container.swap(WhatsAppClient, () => fake as any)

    try {
      const season = await SeasonFactory.merge({ isActive: true }).create()
      const round = await RoundFactory.merge({
        seasonId: season.id,
        status: 'awaiting_pick',
      }).create()
      await RoundMatchCandidateFactory.merge({
        roundId: round.id,
        position: 1,
        externalId: 1001,
        homeTeam: 'Flamengo',
        awayTeam: 'Palmeiras',
        kickoffAt: DateTime.fromISO('2026-05-04T20:00:00Z'),
      }).create()
      await RoundMatchCandidateFactory.merge({
        roundId: round.id,
        position: 2,
        externalId: 1002,
      }).create()
      const admin = await UserFactory.merge({
        whatsappNumber: '5511999999999',
        isAdmin: true,
      }).create()

      const handler = await app.container.make(WhatsAppInboundHandler)
      await handler.handle({
        fromNumber: admin.whatsappNumber,
        text: '/escolher 1',
        messageId: 'm1',
      })

      const roundRepo = await app.container.make(RoundRepository)
      const refreshed = await roundRepo.findByIdOrFail(round.id)
      assert.equal(refreshed.status, 'pending')

      const matchRepo = await app.container.make(MatchRepository)
      const match = await matchRepo.findByRoundId(round.id)
      assert.exists(match)
      assert.equal(match!.homeTeam, 'Flamengo')

      assert.lengthOf(fake.userCalls, 1)
      assert.match(fake.userCalls[0].text, /Flamengo x Palmeiras/)
      assert.lengthOf(fake.groupCalls, 1)
      assert.match(fake.groupCalls[0], /jogo definido pelo admin/i)
    } finally {
      app.container.restore(WhatsAppClient)
    }
  })

  test('user comum → reply restrição, não cria match', async ({ assert }) => {
    const fake = new FakeClient()
    app.container.swap(WhatsAppClient, () => fake as any)

    try {
      const season = await SeasonFactory.merge({ isActive: true }).create()
      const round = await RoundFactory.merge({
        seasonId: season.id,
        status: 'awaiting_pick',
      }).create()
      await RoundMatchCandidateFactory.merge({ roundId: round.id, position: 1 }).create()
      const user = await UserFactory.merge({
        whatsappNumber: '5511888888888',
        isAdmin: false,
      }).create()

      const handler = await app.container.make(WhatsAppInboundHandler)
      await handler.handle({
        fromNumber: user.whatsappNumber,
        text: '/escolher 1',
        messageId: 'm2',
      })

      const matchRepo = await app.container.make(MatchRepository)
      assert.notExists(await matchRepo.findByRoundId(round.id))
      assert.lengthOf(fake.userCalls, 1)
      assert.match(fake.userCalls[0].text, /restrito/i)
    } finally {
      app.container.restore(WhatsAppClient)
    }
  })

  test('sem rodada awaiting → reply "nenhuma rodada"', async ({ assert }) => {
    const fake = new FakeClient()
    app.container.swap(WhatsAppClient, () => fake as any)

    try {
      const admin = await UserFactory.merge({
        whatsappNumber: '5511777777777',
        isAdmin: true,
      }).create()

      const handler = await app.container.make(WhatsAppInboundHandler)
      await handler.handle({
        fromNumber: admin.whatsappNumber,
        text: '/escolher 1',
        messageId: 'm3',
      })

      assert.lengthOf(fake.userCalls, 1)
      assert.match(fake.userCalls[0].text, /nenhuma rodada aguardando/i)
    } finally {
      app.container.restore(WhatsAppClient)
    }
  })

  test('posição inválida → reply', async ({ assert }) => {
    const fake = new FakeClient()
    app.container.swap(WhatsAppClient, () => fake as any)

    try {
      const season = await SeasonFactory.merge({ isActive: true }).create()
      const round = await RoundFactory.merge({
        seasonId: season.id,
        status: 'awaiting_pick',
      }).create()
      await RoundMatchCandidateFactory.merge({ roundId: round.id, position: 1 }).create()
      const admin = await UserFactory.merge({
        whatsappNumber: '5511666666666',
        isAdmin: true,
      }).create()

      const handler = await app.container.make(WhatsAppInboundHandler)
      await handler.handle({
        fromNumber: admin.whatsappNumber,
        text: '/escolher 99',
        messageId: 'm4',
      })

      assert.lengthOf(fake.userCalls, 1)
      assert.match(fake.userCalls[0].text, /posição.*inválida/i)
    } finally {
      app.container.restore(WhatsAppClient)
    }
  })
})
```

- [ ] **Step 2: Rodar — falha (handler não trata `/escolher`)**

Run:
```bash
cd apps/api && pnpm test:functional --files='tests/functional/whatsapp_inbound_escolher.spec.ts'
```
Expected: FAIL.

- [ ] **Step 3: Atualizar `WhatsAppInboundHandler`**

Edite `apps/api/app/services/whatsapp_inbound_handler.ts`. Adicione os imports no topo:

```ts
import RoundCandidateRepository from '#repositories/round_candidate_repository'
import RoundCandidatePickService from '#services/round_candidate_pick_service'
import { adminPickedMessage } from '#integrations/whatsapp/templates/admin_picked'
```

Atualize o construtor da classe pra incluir as deps novas (manter as existentes):

```ts
  constructor(
    private userRepository: UserRepository,
    private roundRepository: RoundRepository,
    private guessRepository: GuessRepository,
    private notifier: WhatsAppNotifier,
    private client: WhatsAppClient,
    private roundCandidateRepository: RoundCandidateRepository,
    private roundCandidatePickService: RoundCandidatePickService
  ) {}
```

Atualize o método `handle`:

```ts
  async handle(msg: IncomingMessage): Promise<void> {
    if (/^\/cadastro\b/i.test(msg.text)) {
      return this.handleRegister(msg)
    }
    if (/^\/escolher\b/i.test(msg.text)) {
      return this.handleEscolher(msg)
    }
    return this.handleGuess(msg)
  }
```

Adicione o handler novo (após `handleRegister`):

```ts
  private async handleEscolher(msg: IncomingMessage): Promise<void> {
    const match = msg.text.match(/^\/escolher\s+(\d+)\s*$/i)
    if (!match) {
      await this.client.sendToUser(
        msg.fromNumber,
        'Uso: /escolher <número da posição>'
      )
      return
    }

    const user = await this.userRepository.findByWhatsappNumber(msg.fromNumber)
    if (!user) {
      await this.client.sendToUser(msg.fromNumber, NOT_REGISTERED)
      return
    }
    if (!user.isAdmin) {
      await this.client.sendToUser(msg.fromNumber, '⛔ Comando restrito a administradores.')
      return
    }

    const round = await this.roundRepository.findCurrentAwaitingPickAcrossSeasons()
    if (!round) {
      await this.client.sendToUser(msg.fromNumber, 'Nenhuma rodada aguardando escolha.')
      return
    }

    const position = parseInt(match[1], 10)
    const candidate = await this.roundCandidateRepository.findByRoundAndPosition(round.id, position)
    if (!candidate) {
      await this.client.sendToUser(
        msg.fromNumber,
        `Posição ${position} é inválida pra rodada atual.`
      )
      return
    }

    await this.roundCandidatePickService.pick(round, candidate)

    try {
      await this.client.sendToUser(
        msg.fromNumber,
        `✅ Jogo da rodada ${round.number} definido: ${candidate.homeTeam} x ${candidate.awayTeam}`
      )
    } catch (err) {
      logger.error({ err }, 'WhatsAppInboundHandler: falha ao mandar reply privado /escolher')
    }

    try {
      await this.client.sendToGroup(
        adminPickedMessage({
          roundNumber: round.number,
          homeTeam: candidate.homeTeam,
          awayTeam: candidate.awayTeam,
        })
      )
    } catch (err) {
      logger.error({ err }, 'WhatsAppInboundHandler: falha ao postar admin pick no grupo')
    }
  }
```

- [ ] **Step 4: Rodar — passa**

Run:
```bash
cd apps/api && pnpm test:functional --files='tests/functional/whatsapp_inbound_escolher.spec.ts'
```
Expected: PASS em todos os 4 casos.

- [ ] **Step 5: Commit**

```bash
git add apps/api/app/services/whatsapp_inbound_handler.ts apps/api/tests/functional/whatsapp_inbound_escolher.spec.ts
git commit -m "feat(whatsapp): add /escolher admin command for tied match pick"
```

---

### Task 3: `OpenRoundJob` — branch `awaiting_pick` (TDD functional)

**Files:**
- Modify: `apps/api/tests/functional/open_round_job.spec.ts`
- Modify: `apps/api/app/jobs/open_round_job.ts`

- [ ] **Step 1: Adicionar testes pro novo branch**

Append em `apps/api/tests/functional/open_round_job.spec.ts` (dentro do `test.group` existente; ajustar imports conforme necessário):

```ts
test('round em awaiting_pick + WhatsApp ready + nenhum poll_message_id → manda poll', async ({
  assert,
}) => {
  const fakeClient = makeFakeWhatsAppClient() // helper local — ver Step 1.1 abaixo
  app.container.swap(WhatsAppClient, () => fakeClient as any)

  // mock football-data: retorna currentMatchday da round em awaiting
  const fakeFootball = {
    async fetchStandings() {
      return {
        season: { startDate: '2026-04-01' },
        standings: [{ type: 'TOTAL', table: [] }],
        filters: { season: '2026' },
        competition: { currentSeason: { currentMatchday: 5 } },
      }
    },
    async fetchMatchesByMatchday() {
      return []
    },
  }
  app.container.swap(FootballDataClient, () => fakeFootball as any)

  try {
    const season = await SeasonFactory.merge({ isActive: true }).create()
    const round = await RoundFactory.merge({
      seasonId: season.id,
      number: 5,
      status: 'awaiting_pick',
    }).create()
    await RoundMatchCandidateFactory.merge({ roundId: round.id, position: 1, homeTeam: 'A', awayTeam: 'B' }).create()
    await RoundMatchCandidateFactory.merge({ roundId: round.id, position: 2, homeTeam: 'C', awayTeam: 'D' }).create()

    const job = await app.container.make(OpenRoundJob)
    await job.run()

    assert.lengthOf(fakeClient.pollCalls, 1)
    assert.match(fakeClient.pollCalls[0].question, /Rodada 5/)

    const candidateRepo = await app.container.make(RoundCandidateRepository)
    const candidates = await candidateRepo.list(round.id)
    assert.exists(candidates[0].pollMessageId)
  } finally {
    app.container.restore(WhatsAppClient)
    app.container.restore(FootballDataClient)
  }
})

test('round em awaiting_pick + algum candidato com poll_message_id → não re-manda', async ({
  assert,
}) => {
  const fakeClient = makeFakeWhatsAppClient()
  app.container.swap(WhatsAppClient, () => fakeClient as any)
  const fakeFootball = {
    async fetchStandings() {
      return {
        season: { startDate: '2026-04-01' },
        standings: [{ type: 'TOTAL', table: [] }],
        filters: { season: '2026' },
        competition: { currentSeason: { currentMatchday: 6 } },
      }
    },
    async fetchMatchesByMatchday() { return [] },
  }
  app.container.swap(FootballDataClient, () => fakeFootball as any)

  try {
    const season = await SeasonFactory.merge({ isActive: true }).create()
    const round = await RoundFactory.merge({
      seasonId: season.id,
      number: 6,
      status: 'awaiting_pick',
    }).create()
    await RoundMatchCandidateFactory.merge({
      roundId: round.id,
      position: 1,
      pollMessageId: 'already-sent',
    }).create()

    const job = await app.container.make(OpenRoundJob)
    await job.run()

    assert.lengthOf(fakeClient.pollCalls, 0)
  } finally {
    app.container.restore(WhatsAppClient)
    app.container.restore(FootballDataClient)
  }
})

test('depois do admin escolher (status=pending + match) → próximo run flipa pra open', async ({
  assert,
}) => {
  const fakeClient = makeFakeWhatsAppClient()
  app.container.swap(WhatsAppClient, () => fakeClient as any)
  const fakeFootball = {
    async fetchStandings() {
      return {
        season: { startDate: '2026-04-01' },
        standings: [{ type: 'TOTAL', table: [] }],
        filters: { season: '2026' },
        competition: { currentSeason: { currentMatchday: 7 } },
      }
    },
    async fetchMatchesByMatchday() { return [] },
  }
  app.container.swap(FootballDataClient, () => fakeFootball as any)

  try {
    const season = await SeasonFactory.merge({ isActive: true }).create()
    const round = await RoundFactory.merge({
      seasonId: season.id,
      number: 7,
      status: 'pending',
    }).create()
    await MatchFactory.merge({
      roundId: round.id,
      kickoffAt: DateTime.now().plus({ hours: 2 }),
    }).create()

    const job = await app.container.make(OpenRoundJob)
    await job.run()

    await round.refresh()
    assert.equal(round.status, 'open')
  } finally {
    app.container.restore(WhatsAppClient)
    app.container.restore(FootballDataClient)
  }
})
```

Imports/helpers a garantir no topo do arquivo (adicionar os que faltarem):

```ts
import { DateTime } from 'luxon'
import WhatsAppClient, {
  type IncomingMessageHandler,
  type PollSendResult,
  type WhatsAppMode,
} from '#integrations/whatsapp/whatsapp_client'
import { RoundMatchCandidateFactory } from '#factories/round_match_candidate_factory'
import { MatchFactory } from '#factories/match_factory'
import RoundCandidateRepository from '#repositories/round_candidate_repository'
import OpenRoundJob from '#jobs/open_round_job'
import FootballDataClient from '#integrations/football_data/client'
```

E o helper local (top do arquivo, abaixo dos imports):

```ts
function makeFakeWhatsAppClient() {
  const fake: any = {
    mode: 'stub' as WhatsAppMode,
    pollCalls: [] as { question: string; options: string[] }[],
    groupCalls: [] as string[],
    userCalls: [] as { to: string; text: string }[],
    async connect() {},
    async disconnect() {},
    isConnected() { return true },
    async sendToGroup(text: string) { fake.groupCalls.push(text) },
    async sendToUser(to: string, text: string) { fake.userCalls.push({ to, text }) },
    async sendPollToGroup(question: string, options: string[]): Promise<PollSendResult> {
      fake.pollCalls.push({ question, options })
      return { messageId: 'fake-poll-id' }
    },
    onMessage(_h: IncomingMessageHandler) {},
  }
  return fake
}
```

- [ ] **Step 2: Rodar — falha**

Run:
```bash
cd apps/api && pnpm test:functional --files='tests/functional/open_round_job.spec.ts'
```
Expected: FAIL nos novos casos (job ignora awaiting_pick).

- [ ] **Step 3: Atualizar `OpenRoundJob`**

Edite `apps/api/app/jobs/open_round_job.ts`. Adicione imports:

```ts
import RoundCandidateRepository from '#repositories/round_candidate_repository'
```

Atualize o construtor com `@inject()` adicionando a dep:

```ts
  constructor(
    private seasonRepository: SeasonRepository,
    private roundRepository: RoundRepository,
    private matchRepository: MatchRepository,
    private fixturesSyncService: FixturesSyncService,
    private notifier: WhatsAppNotifier,
    private userRepository: UserRepository,
    private roundCandidateRepository: RoundCandidateRepository
  ) {}
```

Substitua o corpo do `for (const season of seasons)` no método `run()` pra incluir o branch `awaiting_pick` ANTES do branch `pending` existente:

```ts
    for (const season of seasons) {
      try {
        const syncReport = await this.fixturesSyncService.syncCurrentMatchday(season.id)
        const round = await this.roundRepository.findBySeasonAndNumber(
          season.id,
          syncReport.currentMatchday
        )

        let roundOpened = false

        if (round && round.status === RoundStatus.AWAITING_PICK) {
          if (!this.notifier.isReady()) {
            logger.warn(
              { seasonId: season.id, roundId: round.id },
              'OpenRoundJob: WhatsApp offline — skipping poll send'
            )
          } else {
            const candidates = await this.roundCandidateRepository.list(round.id)
            const alreadySent = candidates.some((c) => c.pollMessageId !== null)
            if (!alreadySent && candidates.length > 0) {
              const result = await this.notifier.notifyTieCandidatesPoll({
                roundNumber: round.number,
                candidates: candidates.map((c) => ({
                  homeTeam: c.homeTeam,
                  awayTeam: c.awayTeam,
                  position: c.position,
                })),
              })
              const sentinel = result.messageId ?? 'fallback-emoji'
              await this.roundCandidateRepository.markPollSent(round.id, sentinel)
            }
          }
          runs.push({ seasonId: season.id, syncReport, roundOpened: false })
          continue
        }

        if (round && round.status === RoundStatus.PENDING && syncReport.match) {
          if (!this.notifier.isReady()) {
            logger.warn(
              { seasonId: season.id, roundId: round.id },
              'OpenRoundJob: WhatsApp offline — skipping flip pending→open'
            )
          } else {
            const match = await this.matchRepository.findByRoundId(round.id)
            if (!match) {
              throw new Error('round.match esperado após sync, mas não encontrado')
            }
            await this.notifier.notifyRoundOpened({
              roundNumber: round.number,
              homeTeam: match.homeTeam,
              awayTeam: match.awayTeam,
              kickoffAt: match.kickoffAt,
              pointsMultiplier: match.pointsMultiplier,
            })
            await this.roundRepository.update(round, { status: RoundStatus.OPEN })
            roundOpened = true

            const users = await this.userRepository.list()
            for (const user of users) {
              try {
                await this.notifier.notifyRoundOpenedToUser({
                  user: {
                    whatsappNumber: user.whatsappNumber,
                    name: user.name,
                    emoji: user.emoji,
                  },
                  roundNumber: round.number,
                  homeTeam: match.homeTeam,
                  awayTeam: match.awayTeam,
                  kickoffAt: match.kickoffAt,
                  pointsMultiplier: match.pointsMultiplier,
                })
              } catch (err) {
                logger.warn(
                  {
                    userId: user.id,
                    err: err instanceof Error ? err.message : String(err),
                  },
                  'OpenRoundJob: falha ao mandar DM pra user'
                )
              }
              await new Promise((r) => setTimeout(r, 1000))
            }
          }
        }

        runs.push({ seasonId: season.id, syncReport, roundOpened })
      } catch (err) {
        const msg = err instanceof Error ? err.message : String(err)
        logger.error({ seasonId: season.id, err: msg }, 'OpenRoundJob: falha em season')
        runs.push({
          seasonId: season.id,
          syncReport: null,
          roundOpened: false,
          error: msg,
        })
      }
    }
```

Os imports de `RoundStatus` (já existente) cobrem `RoundStatus.AWAITING_PICK`.

- [ ] **Step 4: Rodar — passa**

Run:
```bash
cd apps/api && pnpm test:functional --files='tests/functional/open_round_job.spec.ts'
```
Expected: PASS — incluindo idempotência e flip pending→open intacto.

- [ ] **Step 5: Suite completa**

Run:
```bash
cd apps/api && pnpm test
```
Expected: tudo verde.

- [ ] **Step 6: Commit**

```bash
git add apps/api/app/jobs/open_round_job.ts apps/api/tests/functional/open_round_job.spec.ts
git commit -m "feat(jobs): send tie poll once when round is awaiting_pick"
```

---

### Validação final end-to-end

- [ ] **Step 1: Typecheck e suite completa**

Run:
```bash
cd apps/api && pnpm typecheck && pnpm test
```
Expected: limpo + tudo verde.

- [ ] **Step 2: Smoke local manual (opcional)**

1. `nvm use && docker compose up -d`
2. `cd apps/api && node ace migration:run` (se ainda não rodou).
3. Setar `WHATSAPP_MODE=stub` no `.env` da API.
4. Criar season + round + candidatos via REPL/factories e disparar `node ace jobs:run-open-round` (a fake do stub vai logar a poll).
5. Bater `POST /api/v1/rounds/:id/pick-candidate` com Bearer admin.
6. Disparar `node ace jobs:run-open-round` de novo e ver round flippar pra `open`.
