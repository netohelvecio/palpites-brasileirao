# Plano 1.6 — Endpoints Users + Seasons (TDD)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development + superpowers:subagent-driven-development ou superpowers:executing-plans.

**Goal:** Implementar endpoints HTTP de `users` (POST, GET, PATCH) e `seasons` (POST, GET, PATCH, sync-stub) com validação VineJS e autenticação admin.

**Architecture:** Controllers finos + validators. Todos sob `/api/v1` protegidos por middleware `adminAuth`. Endpoint `sync` é stub (implementação real no Plano 2).

**Tech Stack:** Adonis 6, VineJS, Japa api-client.

**Spec:** `docs/superpowers/specs/2026-04-20-palpites-brasileirao-design.md` (seção Endpoints HTTP).

**Pré-requisito:** Planos 1.1–1.5 concluídos.

**Critério de aceitação:** testes funcionais de users e seasons passam; Postman exercita todos os endpoints com sucesso.

> ⚠️ **Commits manuais.**

---

## Task 1: Endpoints Users (TDD)

**Files:**
- Create: `apps/api/tests/functional/users.spec.ts`
- Create: `apps/api/app/validators/user_validator.ts`
- Create: `apps/api/app/controllers/users_controller.ts`
- Modify: `apps/api/start/routes.ts`

- [ ] **Step 1: Escrever testes funcionais**

Arquivo: `apps/api/tests/functional/users.spec.ts`
```ts
import { test } from '@japa/runner'
import testUtils from '@adonisjs/core/services/test_utils'

const HEADERS = { authorization: 'Bearer test-admin-token' }

test.group('Users', (group) => {
  group.each.setup(() => testUtils.db().withGlobalTransaction())

  test('POST /users cria usuário', async ({ client, assert }) => {
    const res = await client
      .post('/api/v1/users')
      .headers(HEADERS)
      .json({ name: 'João', whatsappNumber: '5511999998888', emoji: '⚽', isAdmin: false })
    res.assertStatus(201)
    assert.exists(res.body().id)
    assert.equal(res.body().name, 'João')
  })

  test('POST /users exige bearer token', async ({ client }) => {
    const res = await client.post('/api/v1/users').json({})
    res.assertStatus(401)
  })

  test('POST /users rejeita whatsappNumber duplicado', async ({ client }) => {
    const payload = { name: 'João', whatsappNumber: '5511999998888', emoji: '⚽' }
    await client.post('/api/v1/users').headers(HEADERS).json(payload)
    const res = await client.post('/api/v1/users').headers(HEADERS).json(payload)
    res.assertStatus(422)
  })

  test('GET /users lista', async ({ client, assert }) => {
    await client.post('/api/v1/users').headers(HEADERS).json({
      name: 'João', whatsappNumber: '5511999998888', emoji: '⚽',
    })
    const res = await client.get('/api/v1/users').headers(HEADERS)
    res.assertStatus(200)
    assert.lengthOf(res.body(), 1)
  })

  test('PATCH /users/:id edita', async ({ client, assert }) => {
    const created = await client.post('/api/v1/users').headers(HEADERS).json({
      name: 'João', whatsappNumber: '5511999998888', emoji: '⚽',
    })
    const res = await client.patch(`/api/v1/users/${created.body().id}`).headers(HEADERS).json({ emoji: '🐍' })
    res.assertStatus(200)
    assert.equal(res.body().emoji, '🐍')
  })
})
```

- [ ] **Step 2: Rodar testes — FALHAM**

```bash
cd apps/api
node ace test functional --files='tests/functional/users.spec.ts'
```

- [ ] **Step 3: Criar validator**

Arquivo: `apps/api/app/validators/user_validator.ts`
```ts
import vine from '@vinejs/vine'

export const createUserValidator = vine.compile(
  vine.object({
    name: vine.string().minLength(1).maxLength(120),
    whatsappNumber: vine
      .string()
      .regex(/^\d{10,15}$/)
      .unique(async (db, value) => {
        const row = await db
          .from('users')
          .where('whatsapp_number', value)
          .where('is_deleted', false)
          .first()
        return !row
      }),
    emoji: vine.string().minLength(1).maxLength(10),
    isAdmin: vine.boolean().optional(),
  })
)

export const updateUserValidator = vine.compile(
  vine.object({
    name: vine.string().minLength(1).maxLength(120).optional(),
    emoji: vine.string().minLength(1).maxLength(10).optional(),
    isAdmin: vine.boolean().optional(),
  })
)
```

- [ ] **Step 4: Criar controller**

Arquivo: `apps/api/app/controllers/users_controller.ts`
```ts
import type { HttpContext } from '@adonisjs/core/http'
import User from '#models/user'
import { createUserValidator, updateUserValidator } from '#validators/user_validator'

export default class UsersController {
  async store({ request, response }: HttpContext) {
    const payload = await request.validateUsing(createUserValidator)
    const user = await User.create(payload)
    return response.created(user)
  }

  async index({ response }: HttpContext) {
    const users = await User.query().orderBy('created_at', 'asc')
    return response.ok(users)
  }

  async update({ params, request, response }: HttpContext) {
    const user = await User.findOrFail(params.id)
    const payload = await request.validateUsing(updateUserValidator)
    user.merge(payload)
    await user.save()
    return response.ok(user)
  }
}
```

- [ ] **Step 5: Registrar rotas**

Modificar: `apps/api/start/routes.ts`
```ts
import router from '@adonisjs/core/services/router'
import { middleware } from './kernel.js'

const UsersController = () => import('#controllers/users_controller')

router
  .group(() => {
    router.post('/users', [UsersController, 'store'])
    router.get('/users', [UsersController, 'index'])
    router.patch('/users/:id', [UsersController, 'update'])
  })
  .prefix('/api/v1')
  .use(middleware.adminAuth())
```

- [ ] **Step 6: Rodar testes — PASSAM**

```bash
node ace test functional --files='tests/functional/users.spec.ts'
```

Expected: 5 passing.

---

## Task 2: Endpoints Seasons (TDD)

**Files:**
- Create: `apps/api/tests/functional/seasons.spec.ts`
- Create: `apps/api/app/validators/season_validator.ts`
- Create: `apps/api/app/controllers/seasons_controller.ts`
- Modify: `apps/api/start/routes.ts`

- [ ] **Step 1: Escrever testes**

Arquivo: `apps/api/tests/functional/seasons.spec.ts`
```ts
import { test } from '@japa/runner'
import testUtils from '@adonisjs/core/services/test_utils'

const HEADERS = { authorization: 'Bearer test-admin-token' }
const BASE_PAYLOAD = {
  year: 2026,
  name: 'Brasileirão 2026',
  externalLeagueId: 71,
  isActive: true,
  startsAt: '2026-04-12T00:00:00.000Z',
  endsAt: '2026-12-08T00:00:00.000Z',
}

test.group('Seasons', (group) => {
  group.each.setup(() => testUtils.db().withGlobalTransaction())

  test('POST /seasons cria', async ({ client, assert }) => {
    const res = await client.post('/api/v1/seasons').headers(HEADERS).json(BASE_PAYLOAD)
    res.assertStatus(201)
    assert.equal(res.body().year, 2026)
  })

  test('GET /seasons lista', async ({ client, assert }) => {
    await client.post('/api/v1/seasons').headers(HEADERS).json(BASE_PAYLOAD)
    const res = await client.get('/api/v1/seasons').headers(HEADERS)
    res.assertStatus(200)
    assert.lengthOf(res.body(), 1)
  })

  test('PATCH /seasons/:id edita', async ({ client, assert }) => {
    const created = await client.post('/api/v1/seasons').headers(HEADERS).json({ ...BASE_PAYLOAD, isActive: false })
    const res = await client.patch(`/api/v1/seasons/${created.body().id}`).headers(HEADERS).json({ isActive: true })
    res.assertStatus(200)
    assert.equal(res.body().isActive, true)
  })

  test('POST /seasons/:id/sync retorna 202 (stub)', async ({ client }) => {
    const created = await client.post('/api/v1/seasons').headers(HEADERS).json(BASE_PAYLOAD)
    const res = await client.post(`/api/v1/seasons/${created.body().id}/sync`).headers(HEADERS)
    res.assertStatus(202)
  })
})
```

- [ ] **Step 2: Rodar — FALHAM**

```bash
node ace test functional --files='tests/functional/seasons.spec.ts'
```

- [ ] **Step 3: Criar validator**

Arquivo: `apps/api/app/validators/season_validator.ts`
```ts
import vine from '@vinejs/vine'

export const createSeasonValidator = vine.compile(
  vine.object({
    year: vine.number().min(2000).max(2100),
    name: vine.string().minLength(1).maxLength(120),
    externalLeagueId: vine.number(),
    isActive: vine.boolean(),
    startsAt: vine.date(),
    endsAt: vine.date(),
  })
)

export const updateSeasonValidator = vine.compile(
  vine.object({
    name: vine.string().minLength(1).maxLength(120).optional(),
    isActive: vine.boolean().optional(),
    startsAt: vine.date().optional(),
    endsAt: vine.date().optional(),
  })
)
```

- [ ] **Step 4: Criar controller**

Arquivo: `apps/api/app/controllers/seasons_controller.ts`
```ts
import type { HttpContext } from '@adonisjs/core/http'
import Season from '#models/season'
import { createSeasonValidator, updateSeasonValidator } from '#validators/season_validator'

export default class SeasonsController {
  async store({ request, response }: HttpContext) {
    const payload = await request.validateUsing(createSeasonValidator)
    const season = await Season.create(payload as any)
    return response.created(season)
  }

  async index({ response }: HttpContext) {
    const seasons = await Season.query().orderBy('year', 'desc')
    return response.ok(seasons)
  }

  async update({ params, request, response }: HttpContext) {
    const season = await Season.findOrFail(params.id)
    const payload = await request.validateUsing(updateSeasonValidator)
    season.merge(payload as any)
    await season.save()
    return response.ok(season)
  }

  async sync({ params, response }: HttpContext) {
    // Stub: implementação real da API-Football vem no Plano 2.
    return response.accepted({
      message: 'sync será implementado no plano 2',
      seasonId: params.id,
    })
  }
}
```

- [ ] **Step 5: Registrar rotas**

Modificar: `apps/api/start/routes.ts` — adicionar import:
```ts
const SeasonsController = () => import('#controllers/seasons_controller')
```

E dentro do `.group()`:
```ts
    router.post('/seasons', [SeasonsController, 'store'])
    router.get('/seasons', [SeasonsController, 'index'])
    router.patch('/seasons/:id', [SeasonsController, 'update'])
    router.post('/seasons/:id/sync', [SeasonsController, 'sync'])
```

- [ ] **Step 6: Rodar — PASSAM**

```bash
node ace test functional --files='tests/functional/seasons.spec.ts'
```

Expected: 4 passing.
