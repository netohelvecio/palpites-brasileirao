# Plan 8.1 — Schema + Scoring Core

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar as colunas `points_multiplier` (em `matches`) e `is_exact` (em `guesses`) com backfill, e evoluir `calculatePoints` pra retornar `{ points, isExact }` aplicando o multiplier.

**Architecture:** Migration única adiciona ambas as colunas com defaults seguros. `guess_scoring_service` (puro) ganha 3º parâmetro `multiplier` opcional e passa a retornar um objeto. Modelos Lucid são re-gerados via `migration:run` (gera `database/schema.ts`).

**Tech Stack:** Adonis 7, Lucid migrations, Postgres, Japa (unit tests), TypeScript ESM.

**Spec:** `docs/superpowers/specs/2026-05-02-double-points-round-design.md`

---

### Task 1: Migration — colunas `points_multiplier` e `is_exact`

**Files:**
- Create: `apps/api/database/migrations/0009_add_points_multiplier_and_is_exact.ts`

- [ ] **Step 1: Criar a migration**

```ts
import { BaseSchema } from '@adonisjs/lucid/schema'

export default class extends BaseSchema {
  async up() {
    this.schema.alterTable('matches', (table) => {
      table.smallint('points_multiplier').notNullable().defaultTo(1)
    })
    this.schema.alterTable('guesses', (table) => {
      table.boolean('is_exact').nullable()
    })
    this.defer(async (db) => {
      await db.rawQuery('UPDATE guesses SET is_exact = (points = 3) WHERE points IS NOT NULL')
    })
  }

  async down() {
    this.schema.alterTable('guesses', (table) => {
      table.dropColumn('is_exact')
    })
    this.schema.alterTable('matches', (table) => {
      table.dropColumn('points_multiplier')
    })
  }
}
```

- [ ] **Step 2: Rodar a migration localmente**

Run:
```bash
nvm use && cd apps/api && node ace migration:run
```
Expected: log `migrated 0009_add_points_multiplier_and_is_exact` sem erro.

- [ ] **Step 3: Reformatar `database/schema.ts`**

O codegen do Adonis 7 escreve `$columns` em linha única após `migration:run`; prettier reformata pra multi-linha. Sem isso o lint do CI quebra (gotcha documentado em `apps/api/CLAUDE.md`).

Run:
```bash
cd apps/api && pnpm format
```
Expected: prettier reformata `database/schema.ts` (e quaisquer outros arquivos tocados).

- [ ] **Step 4: Verificar que `MatchSchema` agora tem `pointsMultiplier` e `GuessSchema` tem `isExact`**

Run:
```bash
grep -E "pointsMultiplier|isExact" apps/api/database/schema.ts
```
Expected: ambos os campos aparecem como `declare ...` em suas respectivas classes.

- [ ] **Step 5: Commit**

```bash
git add apps/api/database/migrations/0009_add_points_multiplier_and_is_exact.ts apps/api/database/schema.ts
git commit -m "feat(db): add points_multiplier on matches and is_exact on guesses"
```

---

### Task 2: Atualizar testes existentes do `guess_scoring_service` (TDD — fail first)

**Files:**
- Modify: `apps/api/tests/unit/guess_scoring_service.spec.ts`

- [ ] **Step 1: Reescrever os testes existentes pro novo retorno `{ points, isExact }` + adicionar matriz com multiplier**

Substitua **todo** o conteúdo do arquivo por:

```ts
import { test } from '@japa/runner'
import { calculatePoints } from '#services/guess_scoring_service'

test.group('GuessScoringService — multiplier=1 (default)', () => {
  test('placar exato = 3 pts, isExact=true', ({ assert }) => {
    const r = calculatePoints({ guessHome: 2, guessAway: 1 }, { finalHome: 2, finalAway: 1 })
    assert.deepEqual(r, { points: 3, isExact: true })
  })

  test('acertou vencedor (home) = 1 pt, isExact=false', ({ assert }) => {
    const r = calculatePoints({ guessHome: 3, guessAway: 1 }, { finalHome: 2, finalAway: 0 })
    assert.deepEqual(r, { points: 1, isExact: false })
  })

  test('acertou vencedor (away) = 1 pt', ({ assert }) => {
    const r = calculatePoints({ guessHome: 0, guessAway: 2 }, { finalHome: 1, finalAway: 3 })
    assert.deepEqual(r, { points: 1, isExact: false })
  })

  test('acertou empate sem placar exato = 1 pt', ({ assert }) => {
    const r = calculatePoints({ guessHome: 1, guessAway: 1 }, { finalHome: 2, finalAway: 2 })
    assert.deepEqual(r, { points: 1, isExact: false })
  })

  test('acertou empate com placar exato = 3 pts', ({ assert }) => {
    const r = calculatePoints({ guessHome: 1, guessAway: 1 }, { finalHome: 1, finalAway: 1 })
    assert.deepEqual(r, { points: 3, isExact: true })
  })

  test('errou vencedor = 0 pts', ({ assert }) => {
    const r = calculatePoints({ guessHome: 2, guessAway: 1 }, { finalHome: 0, finalAway: 3 })
    assert.deepEqual(r, { points: 0, isExact: false })
  })

  test('palpitou empate, deu vitória = 0 pts', ({ assert }) => {
    const r = calculatePoints({ guessHome: 1, guessAway: 1 }, { finalHome: 2, finalAway: 0 })
    assert.deepEqual(r, { points: 0, isExact: false })
  })

  test('palpitou vitória, deu empate = 0 pts', ({ assert }) => {
    const r = calculatePoints({ guessHome: 2, guessAway: 1 }, { finalHome: 1, finalAway: 1 })
    assert.deepEqual(r, { points: 0, isExact: false })
  })
})

test.group('GuessScoringService — multiplier=2 (rodada dobrada)', () => {
  test('placar exato = 6 pts, isExact=true', ({ assert }) => {
    const r = calculatePoints({ guessHome: 2, guessAway: 1 }, { finalHome: 2, finalAway: 1 }, 2)
    assert.deepEqual(r, { points: 6, isExact: true })
  })

  test('acertou vencedor = 2 pts, isExact=false', ({ assert }) => {
    const r = calculatePoints({ guessHome: 3, guessAway: 1 }, { finalHome: 2, finalAway: 0 }, 2)
    assert.deepEqual(r, { points: 2, isExact: false })
  })

  test('errou = 0 pts, isExact=false (zero não dobra mesmo com multiplier)', ({ assert }) => {
    const r = calculatePoints({ guessHome: 2, guessAway: 1 }, { finalHome: 0, finalAway: 3 }, 2)
    assert.deepEqual(r, { points: 0, isExact: false })
  })

  test('isExact independe do multiplier — só do palpite vs placar', ({ assert }) => {
    const r1 = calculatePoints({ guessHome: 1, guessAway: 1 }, { finalHome: 1, finalAway: 1 }, 1)
    const r2 = calculatePoints({ guessHome: 1, guessAway: 1 }, { finalHome: 1, finalAway: 1 }, 2)
    assert.equal(r1.isExact, true)
    assert.equal(r2.isExact, true)
  })
})
```

- [ ] **Step 2: Rodar os testes pra confirmar que falham**

Run:
```bash
nvm use && cd apps/api && node ace test unit --files='tests/unit/guess_scoring_service.spec.ts'
```
Expected: **FAIL** — `calculatePoints` ainda retorna `number`, não `{ points, isExact }`. Os asserts com `assert.deepEqual` quebram.

- [ ] **Step 3: Commit dos testes (failing)**

```bash
git add apps/api/tests/unit/guess_scoring_service.spec.ts
git commit -m "test(scoring): expect { points, isExact } return shape with multiplier"
```

---

### Task 3: Implementar novo `calculatePoints` com multiplier

**Files:**
- Modify: `apps/api/app/services/guess_scoring_service.ts`

- [ ] **Step 1: Reescrever a função pura**

Substitua todo o conteúdo de `apps/api/app/services/guess_scoring_service.ts` por:

```ts
export interface GuessScore {
  guessHome: number
  guessAway: number
}

export interface FinalScore {
  finalHome: number
  finalAway: number
}

export interface ScoringResult {
  points: number
  isExact: boolean
}

function outcome(h: number, a: number): 'home' | 'away' | 'draw' {
  if (h > a) return 'home'
  if (a > h) return 'away'
  return 'draw'
}

export function calculatePoints(
  guess: GuessScore,
  final: FinalScore,
  multiplier: number = 1
): ScoringResult {
  const isExact = guess.guessHome === final.finalHome && guess.guessAway === final.finalAway
  if (isExact) return { points: 3 * multiplier, isExact: true }
  if (outcome(guess.guessHome, guess.guessAway) === outcome(final.finalHome, final.finalAway)) {
    return { points: 1 * multiplier, isExact: false }
  }
  return { points: 0, isExact: false }
}
```

- [ ] **Step 2: Rodar os testes do scoring service e confirmar que passam**

Run:
```bash
nvm use && cd apps/api && node ace test unit --files='tests/unit/guess_scoring_service.spec.ts'
```
Expected: **PASS** em todos os 12 cenários (8 multiplier=1 + 4 multiplier=2).

- [ ] **Step 3: Rodar typecheck pra detectar quem quebrou (esperamos `round_finalizer_service.ts` reclamando)**

Run:
```bash
nvm use && cd apps/api && pnpm typecheck
```
Expected: **falha** em `app/services/round_finalizer_service.ts` (usa `calculatePoints({...}, {...})` esperando `number`). É esperado — o finalizer é refatorado no plano 8.3. Anote os erros mas **não tente consertar agora**.

- [ ] **Step 4: Commit da implementação**

```bash
git add apps/api/app/services/guess_scoring_service.ts
git commit -m "feat(scoring): calculatePoints accepts multiplier and returns { points, isExact }"
```

---

### Self-review checklist (executar antes de fechar o plano)

- [ ] Migration usa `defer` pra rodar o backfill UPDATE depois do `alterTable` (Lucid exige isso pra não enfileirar SQL fora de transação de schema)
- [ ] `pnpm format` foi rodado depois do `migration:run`
- [ ] Os testes unit do scoring rodam isolados sem dependência de DB
- [ ] `pnpm typecheck` falha no finalizer (esperado — será resolvido em 8.3)

### Próximo plano

**8.2** — Picker `pickFeaturedMatch` retorna `pointsMultiplier`; `FixturesSyncService` persiste no insert; `MatchesController.upsert` aceita `pointsMultiplier` opcional.
