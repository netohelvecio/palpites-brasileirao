# Plan 6.4 — Backup Script + DuckDNS Updater Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Script `infra/backup.sh` faz `pg_dump` diário do banco de produção, comprime, sobe pra Oracle Object Storage e mantém fallback local de 7 dias. Snippet de cron pra atualização periódica do DNS DuckDNS.

**Architecture:** Bash script único acionado via cron na VM (não em container). Usa `docker compose exec -T postgres pg_dump` pra evitar credenciais hardcoded. Upload via OCI CLI configurado no Plan 6.5. Lifecycle Policy do bucket Object Storage (configurada manualmente) descarta objetos > 30 dias. DuckDNS é só um `curl` em cron de 5 min.

**Tech Stack:** Bash, `pg_dump` (Postgres 16 client), gzip, OCI CLI, cron.

**Spec:** `docs/superpowers/specs/2026-04-27-deploy-design.md`

**Depende de:** Plan 6.1 (compose.prod.yml e Postgres rodando). OCI CLI/bucket são do Plan 6.5 (provisioning).

---

### Task 1: Criar `infra/backup.sh`

**Files:**
- Create: `infra/backup.sh`

- [ ] **Step 1: Conteúdo**

```bash
#!/usr/bin/env bash
set -euo pipefail

# Configurações
COMPOSE_FILE="/opt/palpites/docker-compose.prod.yml"
LOCAL_DIR="/var/backups/palpites"
BUCKET="palpites-backups"
PREFIX="pg/"
RETENTION_DAYS_LOCAL=7

# Setup
TIMESTAMP=$(date -u +%Y%m%d-%H%M%S)
FILENAME="palpites-${TIMESTAMP}.sql.gz"
TMP_FILE="/tmp/${FILENAME}"

mkdir -p "${LOCAL_DIR}"

# Dump + compressão (streaming, sem arquivo intermediário não-comprimido)
docker compose -f "${COMPOSE_FILE}" exec -T postgres \
  pg_dump -U palpites -d palpites_prod --no-owner --no-privileges \
  | gzip -9 > "${TMP_FILE}"

# Sanity: arquivo > 0 bytes
if [ ! -s "${TMP_FILE}" ]; then
  logger -t palpites-backup "FAILED: backup empty for ${TIMESTAMP}"
  exit 1
fi

# Upload pra Object Storage Oracle
NAMESPACE=$(oci os ns get --query 'data' --raw-output)
oci os object put \
  --namespace "${NAMESPACE}" \
  --bucket-name "${BUCKET}" \
  --file "${TMP_FILE}" \
  --name "${PREFIX}${FILENAME}" \
  --force \
  --no-overwrite false

# Move pro fallback local + limpa antigos
mv "${TMP_FILE}" "${LOCAL_DIR}/"
find "${LOCAL_DIR}" -name "palpites-*.sql.gz" -mtime "+${RETENTION_DAYS_LOCAL}" -delete

logger -t palpites-backup "ok: ${FILENAME}"
```

- [ ] **Step 2: Permissão de execução**

```bash
chmod +x infra/backup.sh
```

- [ ] **Step 3: Sanity check de sintaxe Bash**

```bash
bash -n infra/backup.sh
```

Expected: sem output (sintaxe ok).

- [ ] **Step 4: Lint com shellcheck (se instalado)**

```bash
shellcheck infra/backup.sh
```

Expected: sem warnings. Se `shellcheck` não existir local, pular — o `bash -n` já cobre o essencial.

---

### Task 2: Documentar setup OCI CLI + bucket + lifecycle policy

**Files:**
- Create: `docs/runbooks/backup-setup.md`

- [ ] **Step 1: Conteúdo**

```markdown
# Backup setup — palpites-brasileirao

Setup único na VM Oracle. Pré-requisito: VM provisionada (Plan 6.5) e Docker rodando.

## 1. Instalar OCI CLI

```bash
bash -c "$(curl -L https://raw.githubusercontent.com/oracle/oci-cli/master/scripts/install/install.sh)"
```

Ao prompt:
- Install location: default (`/home/deploy/lib/oracle-cli`)
- Add to PATH: yes
- Optional packages: pular

Após instalação:

```bash
exec "$SHELL"
oci --version
```

Expected: versão impressa (ex: `3.42.0`).

## 2. Criar API Key no console Oracle

1. Console Oracle → ícone do usuário (canto superior direito) → **My profile**
2. Resources (esquerda) → **API keys** → **Add API key**
3. Marcar "Generate API key pair" → **Add**
4. **Salvar a chave privada** (download `.pem`) — única chance!
5. Copiar o conteúdo do "Configuration file preview" exibido (vai usar no passo 3)

## 3. Configurar `oci-cli` na VM

```bash
mkdir -p ~/.oci
nano ~/.oci/config         # cola o snippet do "Configuration file preview"
```

Editar a linha `key_file=` apontando pro arquivo `.pem` que você baixou. Subir o `.pem` pra VM:

```bash
# Local (na sua máquina):
scp ~/Downloads/oci_api_key.pem deploy@<VM-IP>:~/.oci/oci_api_key.pem

# Na VM:
chmod 600 ~/.oci/oci_api_key.pem ~/.oci/config
```

Testar:

```bash
oci os ns get
```

Expected: namespace impresso (string tipo `axxxxxxxxxxxx`).

## 4. Criar bucket no Object Storage

```bash
oci os bucket create \
  --name palpites-backups \
  --compartment-id "$(oci iam compartment list --query 'data[0].id' --raw-output)" \
  --public-access-type NoPublicAccess
```

Expected: JSON com detalhes do bucket criado.

## 5. Configurar Lifecycle Policy (retention 30 dias)

```bash
NAMESPACE=$(oci os ns get --query 'data' --raw-output)

cat > /tmp/lifecycle.json <<'EOF'
{
  "items": [
    {
      "name": "expire-old-backups",
      "action": "DELETE",
      "objectNameFilter": { "inclusionPrefixes": ["pg/"] },
      "timeAmount": 30,
      "timeUnit": "DAYS",
      "isEnabled": true
    }
  ]
}
EOF

oci os object-lifecycle-policy put \
  --namespace "${NAMESPACE}" \
  --bucket-name palpites-backups \
  --from-json file:///tmp/lifecycle.json
```

Expected: confirmação JSON.

## 6. Setup do diretório local + cron

```bash
sudo mkdir -p /var/backups/palpites
sudo chown deploy:deploy /var/backups/palpites

echo "0 3 * * * deploy /opt/palpites/infra/backup.sh >> /var/log/palpites-backup.log 2>&1" \
  | sudo tee /etc/cron.d/palpites-backup
sudo chmod 644 /etc/cron.d/palpites-backup
```

Cron roda 03:00 UTC (~00:00 BRT) todo dia.

## 7. Smoke test

```bash
sudo -u deploy /opt/palpites/infra/backup.sh
oci os object list --bucket-name palpites-backups
ls -la /var/backups/palpites/
```

Expected:
- Comando `backup.sh` exit 0.
- Listagem mostra `pg/palpites-<timestamp>.sql.gz` no Object Storage.
- Arquivo idêntico em `/var/backups/palpites/`.

## Restore (procedimento manual)

```bash
# Listar backups disponíveis
oci os object list --bucket-name palpites-backups --prefix pg/

# Baixar um específico
oci os object get \
  --namespace "$(oci os ns get --query 'data' --raw-output)" \
  --bucket-name palpites-backups \
  --name pg/palpites-<timestamp>.sql.gz \
  --file /tmp/restore.sql.gz

# Restaurar (CUIDADO: sobrescreve banco atual)
gunzip -c /tmp/restore.sql.gz | \
  docker compose -f /opt/palpites/docker-compose.prod.yml exec -T postgres \
  psql -U palpites -d palpites_prod
```
```

---

### Task 3: Documentar DuckDNS update cron

**Files:**
- Create: `docs/runbooks/duckdns-setup.md`

- [ ] **Step 1: Conteúdo**

```markdown
# DuckDNS setup

## 1. Criar conta e subdomínio

1. Acessar https://www.duckdns.org/ → login com Google/GitHub
2. Em "domains", criar `palpites-bolao` (ou nome desejado)
3. Apontar pro IP público da VM (campo "current ip")
4. Copiar o **token** exibido no topo da página

## 2. Cron na VM

```bash
TOKEN="<seu-token-duckdns>"
SUBDOMAIN="palpites-bolao"

echo "*/5 * * * * deploy curl -s 'https://www.duckdns.org/update?domains=${SUBDOMAIN}&token=${TOKEN}&ip=' >> /var/log/duckdns.log 2>&1" \
  | sudo tee /etc/cron.d/duckdns
sudo chmod 644 /etc/cron.d/duckdns

sudo touch /var/log/duckdns.log
sudo chown deploy:deploy /var/log/duckdns.log
```

Cron a cada 5 min. `ip=` vazio faz o DuckDNS detectar o IP de origem da requisição automaticamente.

## 3. Validação

```bash
curl "https://www.duckdns.org/update?domains=palpites-bolao&token=${TOKEN}&ip="
# expected: "OK"

dig +short palpites-bolao.duckdns.org
# expected: IP da VM
```
```

---

### Task 4: Commit dos artefatos

- [ ] **Step 1: Stage e commit**

```bash
git add infra/backup.sh \
        docs/runbooks/backup-setup.md \
        docs/runbooks/duckdns-setup.md
```

Mensagem sugerida:

```
infra: add pg_dump backup script and ops runbooks

Backup streams pg_dump → gzip → Oracle Object Storage with 30-day
lifecycle (configured separately), keeps a 7-day local fallback for
recovery from upload failures. Runbooks document OCI CLI bootstrap,
bucket + lifecycle creation, and DuckDNS cron updater.
```

User commita manualmente.

---

## Self-review checklist

- [x] Spec coverage: backup.sh (✓), 30-day lifecycle (✓), 7-day local fallback (✓), DuckDNS cron (✓), runbook de restore (✓).
- [x] `set -euo pipefail` no script.
- [x] Streaming `pg_dump | gzip` evita arquivo intermediário grande.
- [x] Sanity check `[ ! -s "${TMP_FILE}" ]` antes do upload — falha cedo se dump vier vazio.
- [x] Setup do OCI CLI documentado, não código (porque depende de credenciais não-versionáveis).
- [x] Cron files chmod 644 (cron não roda se for 600).
