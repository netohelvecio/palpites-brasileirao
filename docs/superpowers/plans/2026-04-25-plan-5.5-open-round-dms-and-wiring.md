# Plano 5.5 — `OpenRoundJob` DM laço + wiring do handler + validação manual

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans + superpowers:test-driven-development.

**Goal:** Fechar o ciclo: depois do flip + post no grupo no `OpenRoundJob`, iterar `users` e mandar DM personalizada (delay 1s entre cada). Em paralelo, wire o `WhatsAppInboundHandler` no boot via `start/whatsapp.ts` para receber DMs em produção.

**Architecture:**
- `OpenRoundJob` ganha `UserRepository` + chama `notifier.notifyRoundOpenedToUser` em loop após o flip. Falha individual loga warn, segue.
- `start/whatsapp.ts` resolve `WhatsAppInboundHandler` e registra `client.onMessage((msg) => handler.handle(msg))`.
- Validação manual: `pnpm dev` em modo `real`, mandar DM pra o bot do chip, ver palpite registrado + post no grupo.

**Pré-requisito:** Planos 5.1, 5.2, 5.3, 5.4 concluídos.

**Critério de aceitação:**
- Tests existentes do `OpenRoundJob` continuam passando, mais cenários novos: DMs vão pra cada user; falha individual não trava outros.
- `start/whatsapp.ts` atualizado e typecheck OK.
- Suite completa passa.
- Validação manual ok (chip pareado).

> ⚠️ **Commits manuais.**
> ⚠️ **Validação manual** com chip real necessária para confirmar end-to-end.

---

## Task 1: `OpenRoundJob` — atualizar tests com DMs (RED)

**Files:**
- Modify: `apps/api/tests/functional/open_round_job.spec.ts`

- [ ] **Step 1: Adicionar imports e cenários novos**

Adicionar import no topo:

```ts
import { UserFactory } from '#factories/user_factory'
```

Adicionar antes do último `})` do `test.group('OpenRoundJob', ...)`:

```ts
test('DM personalizada pra cada user após flip pra open', async ({ assert }) => {
  const season = await SeasonFactory.merge({
    isActive: true,
    year: 2026,
    externalCompetitionCode: 'BSA',
  }).create()
  await UserFactory.merge({
    whatsappNumber: '5511999990001',
    name: 'Helvécio',
    emoji: '⚽',
  }).create()
  await UserFactory.merge({
    whatsappNumber: '5511999990002',
    name: 'Ana',
    emoji: '🦊',
  }).create()

  const { football, whatsapp } = setupFakes()
  football.standings = fakeStandings(12, 2026, { 1: 30, 2: 25 })
  football.matchesByMatchday.set('2026:12', [fakeMatch(1001, 1, 2, 12)])

  try {
    const job = await app.container.make(OpenRoundJob)
    const report = await job.run()

    assert.equal(report.runs[0].roundOpened, true)
    assert.lengthOf(whatsapp.sentMessages, 1) // grupo

    // 2 users → 2 DMs
    assert.lengthOf(whatsapp.sentDms, 2)
    const numbers = whatsapp.sentDms.map((d) => d.number).sort()
    assert.deepEqual(numbers, ['5511999990001', '5511999990002'])

    const helveccioDm = whatsapp.sentDms.find((d) => d.number === '5511999990001')
    assert.match(helveccioDm!.text, /Oi Helvécio ⚽!/)
    assert.match(helveccioDm!.text, /Rodada 12 aberta/)

    const anaDm = whatsapp.sentDms.find((d) => d.number === '5511999990002')
    assert.match(anaDm!.text, /Oi Ana 🦊!/)
  } finally {
    teardownFakes()
  }
})

test('falha de DM individual não trava outros users nem desfaz flip', async ({ assert }) => {
  const season = await SeasonFactory.merge({
    isActive: true,
    year: 2026,
    externalCompetitionCode: 'BSA',
  }).create()
  await UserFactory.merge({ whatsappNumber: '5511999990003' }).create()
  await UserFactory.merge({ whatsappNumber: '5511999990004' }).create()

  const { football, whatsapp } = setupFakes()
  whatsapp.throwOnSendToUser = new Error('baileys: number blocked')
  football.standings = fakeStandings(12, 2026, { 1: 30, 2: 25 })
  football.matchesByMatchday.set('2026:12', [fakeMatch(1001, 1, 2, 12)])

  try {
    const job = await app.container.make(OpenRoundJob)
    const report = await job.run()

    // grupo flipa normalmente
    assert.equal(report.runs[0].roundOpened, true)
    assert.lengthOf(whatsapp.sentMessages, 1)

    // tentou mandar DM pra cada user mas falharam — nenhum em sentDms
    assert.lengthOf(whatsapp.sentDms, 0)

    // status flipou pra open de qualquer jeito
    const RoundModel = (await import('#models/round')).default
    const round = await RoundModel.query()
      .where('season_id', season.id)
      .where('number', 12)
      .firstOrFail()
    assert.equal(round.status, 'open')
  } finally {
    teardownFakes()
  }
})
```

- [ ] **Step 2: Rodar — FAIL (job ainda não manda DMs)**

```bash
cd apps/api && node ace test functional --files='tests/functional/open_round_job.spec.ts'
```

Expected: 2 novos cenários falham (`sentDms` vazio).

---

## Task 2: `OpenRoundJob` — implementar laço de DMs

**Files:**
- Modify: `apps/api/app/jobs/open_round_job.ts`

- [ ] **Step 1: Atualizar imports e DI**

Adicionar imports:

```ts
import UserRepository from '#repositories/user_repository'
```

E DI no construtor (adicionar `private userRepository: UserRepository`):

```ts
@inject()
export default class OpenRoundJob {
  constructor(
    private seasonRepository: SeasonRepository,
    private roundRepository: RoundRepository,
    private matchRepository: MatchRepository,
    private fixturesSyncService: FixturesSyncService,
    private notifier: WhatsAppNotifier,
    private userRepository: UserRepository
  ) {}
  // ...
}
```

- [ ] **Step 2: Adicionar laço de DMs após o flip**

No bloco do `if (round && round.status === RoundStatus.PENDING && syncReport.match)`, **depois** do `await this.roundRepository.update(...)` e `roundOpened = true`, adicionar:

```ts
const users = await this.userRepository.list()
for (const user of users) {
  try {
    await this.notifier.notifyRoundOpenedToUser({
      user: { whatsappNumber: user.whatsappNumber, name: user.name, emoji: user.emoji },
      roundNumber: round.number,
      homeTeam: match.homeTeam,
      awayTeam: match.awayTeam,
      kickoffAt: match.kickoffAt,
    })
  } catch (err) {
    logger.warn(
      { userId: user.id, err: err instanceof Error ? err.message : String(err) },
      'OpenRoundJob: falha ao mandar DM pra user'
    )
  }
  await new Promise((r) => setTimeout(r, 1000))
}
```

Versão final do bloco (ilustração):

```ts
if (round && round.status === RoundStatus.PENDING && syncReport.match) {
  if (!this.notifier.isReady()) {
    logger.warn(
      { seasonId: season.id, roundId: round.id },
      'OpenRoundJob: WhatsApp offline — skipping flip pending→open'
    )
  } else {
    const match = await this.matchRepository.findByRoundId(round.id)
    if (!match) {
      throw new Error('round.match esperado após sync, mas não encontrado')
    }
    await this.notifier.notifyRoundOpened({
      roundNumber: round.number,
      homeTeam: match.homeTeam,
      awayTeam: match.awayTeam,
      kickoffAt: match.kickoffAt,
    })
    await this.roundRepository.update(round, { status: RoundStatus.OPEN })
    roundOpened = true

    const users = await this.userRepository.list()
    for (const user of users) {
      try {
        await this.notifier.notifyRoundOpenedToUser({
          user: { whatsappNumber: user.whatsappNumber, name: user.name, emoji: user.emoji },
          roundNumber: round.number,
          homeTeam: match.homeTeam,
          awayTeam: match.awayTeam,
          kickoffAt: match.kickoffAt,
        })
      } catch (err) {
        logger.warn(
          { userId: user.id, err: err instanceof Error ? err.message : String(err) },
          'OpenRoundJob: falha ao mandar DM pra user'
        )
      }
      await new Promise((r) => setTimeout(r, 1000))
    }
  }
}
```

- [ ] **Step 3: Rodar — PASSA**

```bash
node ace test functional --files='tests/functional/open_round_job.spec.ts'
```

Expected: 9 passes (7 antigos + 2 novos).

⚠️ **Atenção timing**: o `setTimeout(1000)` no loop adiciona ~1s × N users por test. Em test com 2 users são +2s, ainda dentro do timeout default (30s). Se ficar lento demais, considerar mock do delay — mas sem mock, é o comportamento real.

---

## Task 3: Wire do handler em `start/whatsapp.ts`

**Files:**
- Modify: `apps/api/start/whatsapp.ts`

- [ ] **Step 1: Atualizar conteúdo**

```ts
import app from '@adonisjs/core/services/app'
import logger from '@adonisjs/core/services/logger'
import env from '#start/env'
import WhatsAppClient from '#integrations/whatsapp/whatsapp_client'
import WhatsAppInboundHandler from '#services/whatsapp_inbound_handler'

/**
 * Boot do WhatsApp client + wire do handler de inbound.
 * Preload com environment=['web'] em adonisrc, pra não conectar em test/console/repl.
 *
 * Em modo `real`, conecta no boot e registra o handler. Em `stub`/`disabled`,
 * é no-op barato (handler registrado mas nunca chamado em stub; disabled é offline).
 */
const mode = env.get('WHATSAPP_MODE')
const client = await app.container.make(WhatsAppClient)
const handler = await app.container.make(WhatsAppInboundHandler)

client.onMessage((msg) => handler.handle(msg))

try {
  await client.connect()
  logger.info({ mode }, 'whatsapp: client connected')
} catch (err) {
  logger.error({ err, mode }, 'whatsapp: connect failed')
}

app.terminating(async () => {
  try {
    await client.disconnect()
  } catch (err) {
    logger.warn({ err }, 'whatsapp: disconnect error during shutdown')
  }
})
```

Nota: `client.onMessage(...)` é chamado **antes** de `client.connect()` — isso garante que se o socket já estiver pronto quando subscrever, o handler está pronto. No `BaileysClient`, a subscrição ao `messages.upsert` acontece em `openSocket` (dentro de `connect`), e usa `this.messageHandler` lazy — então a ordem está OK.

- [ ] **Step 2: Typecheck**

```bash
pnpm typecheck
```

Expected: clean.

- [ ] **Step 3: Suite completa**

```bash
node ace test
```

Expected: tudo verde.

---

## Task 4: Validação manual

**Não automatizado.** Validar com chip pareado (mesma sessão da Fase 4).

- [ ] **Step 1: Setup**

```bash
# .env (apps/api/)
WHATSAPP_MODE=real
WHATSAPP_GROUP_JID=<jid do grupo>
WHATSAPP_AUTH_PATH=./storage/whatsapp-auth

cd apps/api && pnpm dev
```

Esperado: `whatsapp: client connected` + `BaileysClient: conexão aberta` no log.

- [ ] **Step 2: Cadastro de novo number**

Mande do seu WhatsApp pessoal (não cadastrado) pro chip do bot:
```
/cadastro Helvécio ⚽
```

Esperado:
- Você recebe DM `"✅ Cadastrado, Helvécio ⚽! A partir de agora você pode mandar palpites."`
- Conferir DB: `SELECT name, emoji, whatsapp_number FROM users WHERE whatsapp_number = '<seu numero>';`

- [ ] **Step 3: Palpite (rodada aberta)**

Garantir que existe round em `status=open` com kickoff futuro (cenário de teste do Plano 4.3 ou setup manual via SQL).

Mande pro bot:
```
2x1 Palmeiras
```

Esperado:
- Você recebe DM `"✅ Palpite registrado: Palmeiras 2 x 1 Flamengo."`
- No grupo aparece: `"Helvécio ⚽ palpitou: Palmeiras 2 x 1 Flamengo"`
- Conferir DB: `SELECT * FROM guesses WHERE user_id = '<seu user id>';`

- [ ] **Step 4: Edição**

Mande:
```
1x1
```

Esperado:
- DM `"✅ Palpite registrado: Palmeiras 1 x 1 Flamengo."`
- Novo post no grupo
- DB: `SELECT * FROM guesses WHERE user_id = '...';` mostra 1 linha só, com 1x1.

- [ ] **Step 5: Caminhos de erro**

Mande:
```
oi tudo bem
```
Esperado: DM `"❌ Não entendi o placar..."`

Avance kickoff pra passado via SQL e mande outro palpite:
```
3x0
```
Esperado: DM `"⏱️ Palpites fechados pra rodada N."`

- [ ] **Step 6: Abertura de rodada com DMs**

Forçar uma round em `pending` no DB e rodar `node ace jobs:run-open-round` (lembrar: parar `pnpm dev` antes — Baileys dual-session conflict). Esperado: cada user cadastrado recebe DM personalizada.

---

## Resumo do Plano 5.5

- `OpenRoundJob` itera todos os users e manda DM personalizada após o flip; falha individual loga warn, segue.
- Delay 1s entre DMs (humanizado simples). Pra 10 users = ~10s extras no job — ok.
- `start/whatsapp.ts` registra `client.onMessage(handler.handle)` antes do connect — handler pronto quando socket abre.
- Validação manual final cobre cadastro, palpite, edit, erros e DM no open.

**Decisões:**
- DMs vêm **após o flip** pra `open`, não antes: o flip + grupo são o "anúncio oficial". Falha em DM não desfaz a abertura.
- Sem retry de DM falhada — o user pode mandar `/cadastro` ou palpite diretamente; receber a DM é nice-to-have.
- Atualizar `CLAUDE.md` (raiz + apps/api) marcando Fase 5 concluída fica como commit separado pós-implementação (mesmo padrão das fases anteriores).
