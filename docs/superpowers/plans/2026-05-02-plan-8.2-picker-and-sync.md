# Plan 8.2 — Picker + Sync + Admin Override

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Detectar 1º × 2º no momento do pick e persistir `points_multiplier` no `matches`, tanto pelo fluxo automático (`FixturesSyncService`) quanto pelo override manual (`MatchesController.upsert`).

**Architecture:** `pickFeaturedMatch` ganha campo `pointsMultiplier` no retorno comparando o pick com `standings[0]`/`standings[1]`. `FixturesSyncService` repassa pro `matchRepository.create`. Validator do upsert aceita o campo opcional, controller persiste o que vier (default 1).

**Tech Stack:** Adonis 7, Lucid, Japa (unit + functional), VineJS, TypeScript ESM.

**Spec:** `docs/superpowers/specs/2026-05-02-double-points-round-design.md`

**Pré-requisito:** Plan 8.1 já mergeado (migration `0009`, scoring service novo).

---

### Task 1: Testes do `featured_match_picker` (TDD — fail first)

**Files:**
- Modify: `apps/api/tests/unit/featured_match_picker.spec.ts`

- [ ] **Step 1: Reescrever testes existentes pra novo retorno + novos casos de multiplier**

Substitua **todo** o conteúdo do arquivo por:

```ts
import { test } from '@japa/runner'
import {
  pickFeaturedMatch,
  type FixtureCandidate,
  type StandingEntry,
} from '#services/featured_match_picker'

const STANDINGS: StandingEntry[] = [
  { teamId: 1, points: 30 },
  { teamId: 2, points: 25 },
  { teamId: 3, points: 20 },
  { teamId: 4, points: 15 },
  { teamId: 5, points: 10 },
]

function fixture(id: number, homeId: number, awayId: number): FixtureCandidate {
  return {
    externalId: id,
    homeTeamId: homeId,
    homeTeamName: `Team${homeId}`,
    awayTeamId: awayId,
    awayTeamName: `Team${awayId}`,
    kickoffAt: new Date('2026-05-04T20:00:00Z'),
  }
}

test.group('FeaturedMatchPicker — escolha do pick', () => {
  test('escolhe o confronto com maior soma de pontos dos dois times', ({ assert }) => {
    const r = pickFeaturedMatch(
      [fixture(100, 3, 4), fixture(101, 1, 2), fixture(102, 5, 1)],
      STANDINGS
    )
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 101)
  })

  test('mantém o primeiro em caso de empate total', ({ assert }) => {
    const r = pickFeaturedMatch(
      [fixture(200, 3, 4), fixture(201, 2, 3)],
      [
        { teamId: 2, points: 15 },
        { teamId: 3, points: 20 },
        { teamId: 4, points: 15 },
      ]
    )
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 200)
  })

  test('times sem registro no standings contam como 0 pts', ({ assert }) => {
    const r = pickFeaturedMatch([fixture(300, 999, 888), fixture(301, 1, 2)], STANDINGS)
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 301)
  })

  test('retorna erro quando lista de fixtures é vazia', ({ assert }) => {
    const r = pickFeaturedMatch([], STANDINGS)
    assert.equal(r.ok, false)
  })

  test('funciona com 1 fixture apenas', ({ assert }) => {
    const r = pickFeaturedMatch([fixture(400, 1, 5)], STANDINGS)
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 400)
  })

  test('standings vazio: todos contam 0, primeiro vence', ({ assert }) => {
    const r = pickFeaturedMatch([fixture(500, 1, 2), fixture(501, 3, 4)], [])
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 500)
  })
})

test.group('FeaturedMatchPicker — pointsMultiplier (rodada dobrada)', () => {
  test('pick é 1º × 2º (home=1, away=2) → multiplier=2', ({ assert }) => {
    const r = pickFeaturedMatch(
      [fixture(101, 1, 2), fixture(102, 3, 4)],
      STANDINGS
    )
    assert.equal(r.ok, true)
    if (r.ok) {
      assert.equal(r.match.externalId, 101)
      assert.equal(r.pointsMultiplier, 2)
    }
  })

  test('pick é 1º × 2º invertido (home=2, away=1) → multiplier=2', ({ assert }) => {
    const r = pickFeaturedMatch(
      [fixture(101, 2, 1), fixture(102, 3, 4)],
      STANDINGS
    )
    assert.equal(r.ok, true)
    if (r.ok) {
      assert.equal(r.match.externalId, 101)
      assert.equal(r.pointsMultiplier, 2)
    }
  })

  test('pick é 1º × 3º → multiplier=1', ({ assert }) => {
    const r = pickFeaturedMatch([fixture(101, 1, 3)], STANDINGS)
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.pointsMultiplier, 1)
  })

  test('pick é 2º × 3º → multiplier=1', ({ assert }) => {
    const r = pickFeaturedMatch([fixture(101, 2, 3)], STANDINGS)
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.pointsMultiplier, 1)
  })

  test('standings com menos de 2 entradas → multiplier=1 (fallback seguro)', ({ assert }) => {
    const r = pickFeaturedMatch([fixture(101, 1, 2)], [{ teamId: 1, points: 5 }])
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.pointsMultiplier, 1)
  })

  test('standings vazio → multiplier=1', ({ assert }) => {
    const r = pickFeaturedMatch([fixture(101, 1, 2)], [])
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.pointsMultiplier, 1)
  })
})
```

- [ ] **Step 2: Rodar e confirmar fail**

Run:
```bash
nvm use && cd apps/api && node ace test unit --files='tests/unit/featured_match_picker.spec.ts'
```
Expected: **FAIL** — `r.pointsMultiplier` é `undefined` no tipo atual.

- [ ] **Step 3: Commit dos testes failing**

```bash
git add apps/api/tests/unit/featured_match_picker.spec.ts
git commit -m "test(picker): expect pointsMultiplier on PickResult for 1x2 detection"
```

---

### Task 2: Implementar `pointsMultiplier` no picker

**Files:**
- Modify: `apps/api/app/services/featured_match_picker.ts`

- [ ] **Step 1: Atualizar o picker**

Substitua **todo** o conteúdo de `apps/api/app/services/featured_match_picker.ts` por:

```ts
export interface FixtureCandidate {
  externalId: number
  homeTeamId: number
  homeTeamName: string
  awayTeamId: number
  awayTeamName: string
  kickoffAt: Date
}

export interface StandingEntry {
  teamId: number
  points: number
}

export type PickResult =
  | { ok: true; match: FixtureCandidate; pointsMultiplier: number }
  | { ok: false; reason: string }

export function pickFeaturedMatch(
  fixtures: FixtureCandidate[],
  standings: StandingEntry[]
): PickResult {
  if (fixtures.length === 0) {
    return { ok: false, reason: 'nenhum jogo disponível para a rodada' }
  }

  const pointsByTeam = new Map(standings.map((s) => [s.teamId, s.points]))
  const pointsOf = (teamId: number) => pointsByTeam.get(teamId) ?? 0

  let best: { match: FixtureCandidate; sum: number } | null = null
  for (const fixture of fixtures) {
    const sum = pointsOf(fixture.homeTeamId) + pointsOf(fixture.awayTeamId)
    if (!best || sum > best.sum) {
      best = { match: fixture, sum }
    }
  }

  const top1 = standings[0]?.teamId
  const top2 = standings[1]?.teamId
  const isOneVsTwo =
    top1 !== undefined &&
    top2 !== undefined &&
    ((best!.match.homeTeamId === top1 && best!.match.awayTeamId === top2) ||
      (best!.match.homeTeamId === top2 && best!.match.awayTeamId === top1))

  return {
    ok: true,
    match: best!.match,
    pointsMultiplier: isOneVsTwo ? 2 : 1,
  }
}
```

- [ ] **Step 2: Rodar testes do picker — todos verdes**

Run:
```bash
nvm use && cd apps/api && node ace test unit --files='tests/unit/featured_match_picker.spec.ts'
```
Expected: **PASS** em todos os cenários.

- [ ] **Step 3: Commit**

```bash
git add apps/api/app/services/featured_match_picker.ts
git commit -m "feat(picker): return pointsMultiplier=2 when pick is 1st vs 2nd"
```

---

### Task 3: `FixturesSyncService` persiste `pointsMultiplier`

**Files:**
- Modify: `apps/api/app/services/fixtures_sync_service.ts`
- Modify: `apps/api/tests/functional/seasons.spec.ts`

> O sync é exposto via `POST /api/v1/seasons/:id/sync` (`SeasonsController`), e os testes vivem em `seasons.spec.ts` usando `FakeFootballDataClient` (helper em `tests/helpers/football_data_mock.ts`). Vamos adicionar 2 casos lá.

- [ ] **Step 1: Adicionar testes failing em `seasons.spec.ts`**

Em `apps/api/tests/functional/seasons.spec.ts`, dentro do `test.group(...)` existente (depois do teste `POST /seasons/:id/sync sem fixtures retorna reason`), adicione:

```ts
test('POST /seasons/:id/sync grava pointsMultiplier=2 quando pick é 1ºx2º', async ({
  client,
  assert,
}) => {
  const season = await SeasonFactory.merge({
    year: 2026,
    externalCompetitionCode: 'BSA',
  }).create()

  const fake = new FakeFootballDataClient()
  // 1º = time 10 (30 pts), 2º = time 20 (25 pts), 3º = time 30 (20 pts)
  fake.standings = fakeStandings(7, 2026, { 10: 30, 20: 25, 30: 20 })
  // Pick por max-soma: 10 × 20 (55 pts), também coincide com 1º × 2º
  fake.matchesByMatchday.set('2026:7', [fakeMatch(901, 10, 20, 7), fakeMatch(902, 30, 40, 7)])

  app.container.swap(FootballDataClient, () => fake as any)
  try {
    const res = await client.post(`/api/v1/seasons/${season.id}/sync`).headers(HEADERS)
    res.assertStatus(200)
    assert.equal(res.body().created, true)
    assert.equal(res.body().match.pointsMultiplier, 2)
  } finally {
    app.container.restore(FootballDataClient)
  }
})

test('POST /seasons/:id/sync grava pointsMultiplier=1 quando pick não é 1ºx2º', async ({
  client,
  assert,
}) => {
  const season = await SeasonFactory.merge({
    year: 2026,
    externalCompetitionCode: 'BSA',
  }).create()

  const fake = new FakeFootballDataClient()
  fake.standings = fakeStandings(8, 2026, { 10: 30, 20: 25, 30: 20, 40: 15 })
  // Pick por max-soma: 10 × 30 (50 pts) — NÃO é 1º × 2º
  fake.matchesByMatchday.set('2026:8', [fakeMatch(901, 10, 30, 8), fakeMatch(902, 20, 40, 8)])

  app.container.swap(FootballDataClient, () => fake as any)
  try {
    const res = await client.post(`/api/v1/seasons/${season.id}/sync`).headers(HEADERS)
    res.assertStatus(200)
    assert.equal(res.body().match.pointsMultiplier, 1)
  } finally {
    app.container.restore(FootballDataClient)
  }
})
```

> **Nota**: o assert `res.body().match.pointsMultiplier` depende do response do controller serializar o match com o campo. Hoje `SeasonsController` provavelmente retorna `presentMatch(match)` ou o model Lucid direto — em ambos os casos `pointsMultiplier` aparece (Lucid serializa colunas com naming strategy snake→camel; o presentMatch passa a expor explicitamente no plan 8.3). Funciona nos dois.

- [ ] **Step 2: Rodar — confirmar fail**

Run:
```bash
nvm use && cd apps/api && node ace test functional --files='tests/functional/seasons.spec.ts'
```
Expected: **FAIL** — sem persistência do `pointsMultiplier`, o valor cai pro DEFAULT 1, e o teste do caso 1×2 quebra (`assert.equal(match.pointsMultiplier, 2)` recebe 1).

- [ ] **Step 3: Atualizar `FixturesSyncService` pra persistir o multiplier**

Em `apps/api/app/services/fixtures_sync_service.ts`, dentro da `db.transaction` em `syncCurrentMatchday`, modifique a chamada `this.matchRepository.create({...})` pra incluir `pointsMultiplier`:

```ts
return this.matchRepository.create(
  {
    roundId: round.id,
    externalId: pick.match.externalId,
    homeTeam: pick.match.homeTeamName,
    awayTeam: pick.match.awayTeamName,
    kickoffAt: DateTime.fromJSDate(pick.match.kickoffAt),
    status: MatchStatus.SCHEDULED,
    pointsMultiplier: pick.pointsMultiplier,
  },
  trx
)
```

- [ ] **Step 4: Rodar testes — verde**

Run:
```bash
nvm use && cd apps/api && node ace test functional --files='tests/functional/seasons.spec.ts'
```
Expected: **PASS** nos 2 testes novos + os 4 existentes continuam passando.

- [ ] **Step 5: Commit**

```bash
git add apps/api/app/services/fixtures_sync_service.ts apps/api/tests/functional/seasons.spec.ts
git commit -m "feat(sync): persist pointsMultiplier from picker on match insert"
```

---

### Task 4: Admin override aceita `pointsMultiplier` opcional

**Files:**
- Modify: `apps/api/app/validators/match_validator.ts`
- Modify: `apps/api/app/controllers/matches_controller.ts`
- Modify: `apps/api/tests/functional/matches.spec.ts`

- [ ] **Step 1: Adicionar test functional (TDD failing)**

No arquivo `apps/api/tests/functional/matches.spec.ts`, adicione dentro do `test.group('Matches', ...)` (depois do teste `PUT /rounds/:roundId/match cria jogo`):

```ts
test('PUT /rounds/:roundId/match aceita pointsMultiplier no payload', async ({
  client,
  assert,
}) => {
  const round = await RoundFactory.with('season').create()
  const res = await client.put(`/api/v1/rounds/${round.id}/match`).headers(HEADERS).json({
    externalId: 9999,
    homeTeam: 'A',
    awayTeam: 'B',
    kickoffAt: '2026-04-20T20:00:00.000Z',
    pointsMultiplier: 2,
  })
  res.assertStatus(200)
  assert.equal(res.body().pointsMultiplier, 2)
})

test('PUT /rounds/:roundId/match sem pointsMultiplier → default 1', async ({
  client,
  assert,
}) => {
  const round = await RoundFactory.with('season').create()
  const res = await client.put(`/api/v1/rounds/${round.id}/match`).headers(HEADERS).json({
    externalId: 8888,
    homeTeam: 'X',
    awayTeam: 'Y',
    kickoffAt: '2026-04-20T20:00:00.000Z',
  })
  res.assertStatus(200)
  assert.equal(res.body().pointsMultiplier, 1)
})

test('PUT /rounds/:roundId/match com pointsMultiplier inválido (>10) → 422', async ({
  client,
}) => {
  const round = await RoundFactory.with('season').create()
  const res = await client.put(`/api/v1/rounds/${round.id}/match`).headers(HEADERS).json({
    externalId: 7777,
    homeTeam: 'X',
    awayTeam: 'Y',
    kickoffAt: '2026-04-20T20:00:00.000Z',
    pointsMultiplier: 99,
  })
  res.assertStatus(422)
})
```

> **Nota**: o segundo teste (`pointsMultiplier` no body do response) depende do `presentMatch` expor o campo. Como o controller hoje retorna o model Lucid direto (`response.ok(match)`) e o model **já tem** `pointsMultiplier` (gerado pelo schema codegen no plan 8.1), o JSON serializado já inclui o campo. No plan 8.3 vamos passar a serializar via `presentMatch`, mas o teste continua válido.

- [ ] **Step 2: Rodar — confirmar fail**

Run:
```bash
nvm use && cd apps/api && node ace test functional --files='tests/functional/matches.spec.ts'
```
Expected: **FAIL** — validator não aceita `pointsMultiplier`, vai retornar 422 nos casos válidos.

- [ ] **Step 3: Atualizar o validator**

Substitua `apps/api/app/validators/match_validator.ts` por:

```ts
import vine from '@vinejs/vine'

export const upsertMatchValidator = vine.create({
  externalId: vine.number(),
  homeTeam: vine.string().minLength(1).maxLength(80),
  awayTeam: vine.string().minLength(1).maxLength(80),
  kickoffAt: vine.date({ formats: ['iso8601'] }),
  pointsMultiplier: vine.number().min(1).max(10).optional(),
})
```

- [ ] **Step 4: Atualizar o controller pra persistir o campo**

Em `apps/api/app/controllers/matches_controller.ts`, ajuste o método `upsert` pra normalizar o multiplier:

```ts
async upsert({ params, request, response }: HttpContext) {
  const round = await this.roundRepository.findByIdOrFail(params.roundId)
  const payload = await request.validateUsing(upsertMatchValidator)
  const pointsMultiplier = payload.pointsMultiplier ?? 1

  const existing = await this.matchRepository.findByRoundId(round.id)
  if (existing) {
    await this.matchRepository.update(existing, {
      externalId: payload.externalId,
      homeTeam: payload.homeTeam,
      awayTeam: payload.awayTeam,
      kickoffAt: payload.kickoffAt,
      status: MatchStatus.SCHEDULED,
      homeScore: null,
      awayScore: null,
      pointsMultiplier,
    })
    return response.ok(existing)
  }

  const match = await this.matchRepository.create({
    roundId: round.id,
    externalId: payload.externalId,
    homeTeam: payload.homeTeam,
    awayTeam: payload.awayTeam,
    kickoffAt: payload.kickoffAt,
    status: MatchStatus.SCHEDULED,
    pointsMultiplier,
  })
  return response.ok(match)
}
```

> **Nota**: `payload.kickoffAt` é `Date` (vine retorna Date pra `vine.date`), mas Lucid `kickoffAt` é `DateTime`. Verifique a tipagem do payload — se quebrar, embrulhe com `DateTime.fromJSDate(payload.kickoffAt)`. (O código atual usa `...payload` direto, então provavelmente já há coerção; observe os erros de typecheck e ajuste se necessário.)

- [ ] **Step 5: Rodar — verde**

Run:
```bash
nvm use && cd apps/api && node ace test functional --files='tests/functional/matches.spec.ts'
```
Expected: **PASS** em todos os 3 casos novos + os anteriores.

- [ ] **Step 6: Commit**

```bash
git add apps/api/app/validators/match_validator.ts apps/api/app/controllers/matches_controller.ts apps/api/tests/functional/matches.spec.ts
git commit -m "feat(api): admin override accepts optional pointsMultiplier (default 1)"
```

---

### Self-review checklist

- [ ] `pickFeaturedMatch` ainda passa nos 6 testes pré-existentes (sem regressão de comportamento de pick)
- [ ] Picker testes cobrem 1×2 em ambas as ordens (home/away invertidos), 1×3, 2×3, e standings curto
- [ ] Sync service testes usam `FakeFootballDataClient` (não rede real)
- [ ] Validator do upsert tem range `min(1).max(10)` pra evitar valores absurdos
- [ ] Controller passa `pointsMultiplier` tanto em create quanto em update (override de match existente)
- [ ] Sem regressão: o teste original `PUT /rounds/:roundId/match cria jogo` ainda passa (sem `pointsMultiplier` no payload)

### Próximo plano

**8.3** — `RoundFinalizerService` aplica multiplier via novo `calculatePoints`; aggregação usa `is_exact`; `MatchView` em `@palpites/shared` ganha o campo; `presentMatch` expõe.
