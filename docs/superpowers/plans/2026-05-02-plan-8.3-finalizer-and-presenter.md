# Plan 8.3 — Finalizer + MatchView + Presenter

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aplicar o multiplier no `RoundFinalizerService` (via novo `calculatePoints`), persistir `is_exact` em cada guess, mudar a agregação de `exactScoresCount` pra usar `is_exact`, e expor `pointsMultiplier` em `MatchView` (shared) + `presentMatch` + `FinalizePreview`.

**Architecture:** Single source of truth — finalizer chama `calculatePoints(guess, final, match.pointsMultiplier)` e persiste `{ points, isExact }`. Agregação por temporada usa `is_exact = true`. `MatchView` ganha o campo (não opcional, default 1) e os controllers que retornam match passam a usar `presentMatch`.

**Tech Stack:** Adonis 7, Lucid, `@palpites/shared` (build-on-install), Japa functional, TypeScript ESM.

**Spec:** `docs/superpowers/specs/2026-05-02-double-points-round-design.md`

**Pré-requisitos:** Plans 8.1 e 8.2 mergeados (migration + scoring service novo + sync persistindo).

---

### Task 1: Atualizar `MatchView` em `@palpites/shared` + presenter

**Files:**
- Modify: `packages/shared/src/views.ts`
- Modify: `apps/api/app/presenters/match_presenter.ts`
- Modify: `apps/api/app/controllers/matches_controller.ts`

- [ ] **Step 1: Adicionar `pointsMultiplier` no `MatchView`**

Em `packages/shared/src/views.ts`, atualize a interface:

```ts
export interface MatchView {
  id: string
  homeTeam: string
  awayTeam: string
  /** ISO 8601 string */
  kickoffAt: string
  homeScore: number | null
  awayScore: number | null
  status: MatchStatus
  pointsMultiplier: number
}
```

- [ ] **Step 2: Rebuild do `@palpites/shared`**

O `apps/api` consome via `dist/`, então a edição precisa ser buildada. (Gotcha documentado em `packages/shared/CLAUDE.md`.)

Run:
```bash
nvm use && pnpm --filter @palpites/shared build
```
Expected: `tsc` regenera `packages/shared/dist/views.d.ts` e `dist/views.js` com o campo novo.

- [ ] **Step 3: Atualizar `presentMatch` pra propagar o campo**

Substitua `apps/api/app/presenters/match_presenter.ts` por:

```ts
import type { MatchView } from '@palpites/shared'
import type Match from '#models/match'

export function presentMatch(match: Match): MatchView {
  return {
    id: match.id,
    homeTeam: match.homeTeam,
    awayTeam: match.awayTeam,
    kickoffAt: match.kickoffAt.toISO()!,
    homeScore: match.homeScore,
    awayScore: match.awayScore,
    status: match.status,
    pointsMultiplier: match.pointsMultiplier,
  }
}
```

- [ ] **Step 4: `MatchesController` passa a usar `presentMatch` nos retornos**

Em `apps/api/app/controllers/matches_controller.ts`, substitua os `response.ok(match)` / `response.ok(existing)` pra serializar via presenter. Adicione o import no topo:

```ts
import { presentMatch } from '#presenters/match_presenter'
```

E ajuste os 3 retornos de match:

```ts
// show:
return response.ok(presentMatch(match))

// upsert (existing branch):
return response.ok(presentMatch(existing))

// upsert (create branch):
return response.ok(presentMatch(match))
```

`refreshScore` retorna `report` (não match), então fica como está.

- [ ] **Step 5: Rodar typecheck + testes funcionais de matches**

Run:
```bash
nvm use && cd apps/api && pnpm typecheck && node ace test functional --files='tests/functional/matches.spec.ts'
```
Expected: typecheck passa (assumindo que o finalizer ainda quebra do plan 8.1 — esse é o próximo task); testes de matches passam (`pointsMultiplier` agora vem via `presentMatch`).

> **Se typecheck falhar fora do `round_finalizer_service.ts`**, ajuste antes de continuar.

- [ ] **Step 6: Commit**

```bash
git add packages/shared/src/views.ts packages/shared/dist apps/api/app/presenters/match_presenter.ts apps/api/app/controllers/matches_controller.ts
git commit -m "feat(shared): expose pointsMultiplier in MatchView and presentMatch"
```

---

### Task 2: Testes do `RoundFinalizerService` com multiplier (TDD failing)

**Files:**
- Modify: `apps/api/tests/functional/round_finalizer_service.spec.ts`

- [ ] **Step 1: Adicionar 3 testes novos cobrindo multiplier=2**

No arquivo `apps/api/tests/functional/round_finalizer_service.spec.ts`, adicione dentro do `test.group('RoundFinalizerService', ...)`:

```ts
test('finalize com pointsMultiplier=2: pontos dobrados, isExact correto', async ({
  assert,
}) => {
  const season = await SeasonFactory.create()
  const round = await RoundFactory.merge({ seasonId: season.id, status: 'closed' }).create()
  const match = await MatchFactory.merge({
    roundId: round.id,
    status: 'finished',
    homeScore: 2,
    awayScore: 1,
    pointsMultiplier: 2,
  }).create()

  const [userExact, userWinner, userWrong] = await UserFactory.createMany(3)
  await Guess.createMany([
    { matchId: match.id, userId: userExact.id, homeScore: 2, awayScore: 1 },
    { matchId: match.id, userId: userWinner.id, homeScore: 3, awayScore: 0 },
    { matchId: match.id, userId: userWrong.id, homeScore: 0, awayScore: 2 },
  ])

  const finalizer = await app.container.make(RoundFinalizerService)
  await finalizer.finalize(round.id)

  const guesses = await Guess.query().where('match_id', match.id)
  const byUser = Object.fromEntries(guesses.map((g) => [g.userId, g]))
  assert.equal(byUser[userExact.id].points, 6)
  assert.equal(byUser[userExact.id].isExact, true)
  assert.equal(byUser[userWinner.id].points, 2)
  assert.equal(byUser[userWinner.id].isExact, false)
  assert.equal(byUser[userWrong.id].points, 0)
  assert.equal(byUser[userWrong.id].isExact, false)
})

test('finalize dobrado: scores agregados refletem soma dobrada e exactScoresCount via is_exact', async ({
  assert,
}) => {
  const season = await SeasonFactory.create()
  const round = await RoundFactory.merge({ seasonId: season.id, status: 'closed' }).create()
  const match = await MatchFactory.merge({
    roundId: round.id,
    status: 'finished',
    homeScore: 2,
    awayScore: 1,
    pointsMultiplier: 2,
  }).create()
  const user = await UserFactory.create()
  await Guess.create({ matchId: match.id, userId: user.id, homeScore: 2, awayScore: 1 })

  const finalizer = await app.container.make(RoundFinalizerService)
  await finalizer.finalize(round.id)

  const score = await Score.query()
    .where('user_id', user.id)
    .where('season_id', season.id)
    .first()
  assert.isNotNull(score)
  assert.equal(score!.totalPoints, 6)
  assert.equal(score!.exactScoresCount, 1)
})

test('previewFinalize com pointsMultiplier=2: roundScores e seasonRanking dobrados', async ({
  assert,
}) => {
  const season = await SeasonFactory.create()
  const round = await RoundFactory.merge({ seasonId: season.id, status: 'closed' }).create()
  const match = await MatchFactory.merge({
    roundId: round.id,
    status: 'finished',
    homeScore: 1,
    awayScore: 0,
    pointsMultiplier: 2,
  }).create()
  const user = await UserFactory.create()
  await Guess.create({ matchId: match.id, userId: user.id, homeScore: 1, awayScore: 0 })

  const finalizer = await app.container.make(RoundFinalizerService)
  const preview = await finalizer.previewFinalize(round.id)

  assert.equal(preview.pointsMultiplier, 2)
  assert.equal(preview.roundScores[0].points, 6)
  assert.equal(preview.seasonRanking[0].totalPoints, 6)
  assert.equal(preview.seasonRanking[0].exactScoresCount, 1)
})
```

- [ ] **Step 2: Rodar — confirmar fail**

Run:
```bash
nvm use && cd apps/api && node ace test functional --files='tests/functional/round_finalizer_service.spec.ts'
```
Expected: **FAIL** — finalizer ainda usa `calculatePoints` antigo (que depois do plan 8.1 retorna `{points, isExact}`); typecheck do finalizer está quebrado desde o plan 8.1, então pode falhar antes de rodar.

- [ ] **Step 3: Commit dos testes**

```bash
git add apps/api/tests/functional/round_finalizer_service.spec.ts
git commit -m "test(finalizer): cover doubled rounds (multiplier=2) end-to-end"
```

---

### Task 3: Refatorar `RoundFinalizerService` pra usar novo `calculatePoints`

**Files:**
- Modify: `apps/api/app/services/round_finalizer_service.ts`

- [ ] **Step 1: Atualizar `previewFinalize` e `finalize`**

Substitua `apps/api/app/services/round_finalizer_service.ts` por:

```ts
import { inject } from '@adonisjs/core'
import db from '@adonisjs/lucid/services/db'
import { MatchStatus, RoundStatus } from '@palpites/shared'
import { calculatePoints } from '#services/guess_scoring_service'
import { sortRanking, type RankingEntry } from '#services/ranking_service'
import GuessRepository from '#repositories/guess_repository'
import MatchRepository from '#repositories/match_repository'
import RoundRepository from '#repositories/round_repository'
import ScoreRepository from '#repositories/score_repository'
import type Match from '#models/match'
import type Round from '#models/round'
import type { RoundScoreEntry } from '#integrations/whatsapp/templates/types'

export interface FinalizePreview {
  pointsMultiplier: number
  roundScores: RoundScoreEntry[]
  seasonRanking: RankingEntry[]
}

@inject()
export default class RoundFinalizerService {
  constructor(
    private roundRepository: RoundRepository,
    private matchRepository: MatchRepository,
    private guessRepository: GuessRepository,
    private scoreRepository: ScoreRepository
  ) {}

  async previewFinalize(roundId: string): Promise<FinalizePreview> {
    const { round, match } = await this.loadFinishedContext(roundId)
    const final = { finalHome: match.homeScore!, finalAway: match.awayScore! }
    const multiplier = match.pointsMultiplier
    const guesses = await this.guessRepository.listByMatchId(match.id)

    const roundScores: RoundScoreEntry[] = guesses
      .map((g) => {
        const r = calculatePoints(
          { guessHome: g.homeScore, guessAway: g.awayScore },
          final,
          multiplier
        )
        return {
          userId: g.userId,
          name: g.user.name,
          emoji: g.user.emoji,
          points: r.points,
        }
      })
      .sort((a, b) => b.points - a.points)

    const exactByUser = new Map(
      guesses.map((g) => [
        g.userId,
        calculatePoints(
          { guessHome: g.homeScore, guessAway: g.awayScore },
          final,
          multiplier
        ).isExact,
      ])
    )

    const seasonId = round.seasonId
    const currentScores = await this.scoreRepository.listBySeasonWithUser(seasonId)
    const byUserId = new Map(currentScores.map((s) => [s.userId, s]))

    const ranking: RankingEntry[] = []
    for (const score of currentScores) {
      const delta = roundScores.find((rs) => rs.userId === score.userId)
      const deltaPoints = delta?.points ?? 0
      const deltaExact = exactByUser.get(score.userId) ? 1 : 0
      ranking.push({
        userId: score.userId,
        name: score.user.name,
        emoji: score.user.emoji,
        totalPoints: score.totalPoints + deltaPoints,
        exactScoresCount: score.exactScoresCount + deltaExact,
      })
    }
    for (const rs of roundScores) {
      if (byUserId.has(rs.userId)) continue
      ranking.push({
        userId: rs.userId,
        name: rs.name,
        emoji: rs.emoji,
        totalPoints: rs.points,
        exactScoresCount: exactByUser.get(rs.userId) ? 1 : 0,
      })
    }

    return {
      pointsMultiplier: multiplier,
      roundScores,
      seasonRanking: sortRanking(ranking),
    }
  }

  async finalize(roundId: string): Promise<void> {
    const { round, match } = await this.loadFinishedContext(roundId)
    const final = { finalHome: match.homeScore!, finalAway: match.awayScore! }
    const multiplier = match.pointsMultiplier
    const guesses = await this.guessRepository.listByMatchId(match.id)

    await db.transaction(async (trx) => {
      const affectedUserIds = new Set<string>()
      for (const guess of guesses) {
        const { points, isExact } = calculatePoints(
          { guessHome: guess.homeScore, guessAway: guess.awayScore },
          final,
          multiplier
        )
        await this.guessRepository.update(guess, { points, isExact }, trx)
        affectedUserIds.add(guess.userId)
      }

      const seasonId = round.seasonId
      for (const userId of affectedUserIds) {
        const userGuesses = await this.guessRepository.listBySeasonAndUser(seasonId, userId, trx)
        const totalPoints = userGuesses.reduce((acc, g) => acc + (g.points ?? 0), 0)
        const exactScoresCount = userGuesses.filter((g) => g.isExact === true).length
        await this.scoreRepository.upsert(userId, seasonId, { totalPoints, exactScoresCount }, trx)
      }

      await this.roundRepository.update(round, { status: RoundStatus.FINISHED }, trx)
    })
  }

  private async loadFinishedContext(roundId: string): Promise<{ round: Round; match: Match }> {
    const round = await this.roundRepository.findByIdOrFail(roundId)
    const match = await this.matchRepository.findByRoundId(round.id)
    if (!match || match.status !== MatchStatus.FINISHED) {
      throw new Error('match não finalizado — não é possível finalizar o round')
    }
    if (match.homeScore === null || match.awayScore === null) {
      throw new Error('match finalizado mas sem placar — bug no provider/refresh')
    }
    return { round, match }
  }
}
```

Mudanças-chave:
- Aceita 3º arg `multiplier` em `calculatePoints`
- Persiste `{ points, isExact }` no `update`
- `exactScoresCount` agregado via `g.isExact === true` (não mais `g.points === 3`)
- `previewFinalize` retorna `pointsMultiplier` no envelope
- `previewFinalize` calcula `isExact` per user (necessário pra `seasonRanking` delta)

- [ ] **Step 2: Rodar typecheck**

Run:
```bash
nvm use && cd apps/api && pnpm typecheck
```
Expected: **PASS** — finalizer agora compatível com o novo `calculatePoints`.

- [ ] **Step 3: Rodar testes do finalizer (todos)**

Run:
```bash
nvm use && cd apps/api && node ace test functional --files='tests/functional/round_finalizer_service.spec.ts'
```
Expected: **PASS** — 8 testes pré-existentes (multiplier default 1) + 3 testes novos (multiplier=2).

- [ ] **Step 4: Rodar a suite completa pra detectar regressão fora do finalizer**

Run:
```bash
nvm use && cd apps/api && node ace test
```
Expected: **PASS** geral. Se quebrar em `sync_scores_job.spec.ts` ou `ranking.spec.ts`, é por algo que mexeu na agregação de `exactScoresCount`. Investigue.

- [ ] **Step 5: Commit**

```bash
git add apps/api/app/services/round_finalizer_service.ts
git commit -m "feat(finalizer): apply pointsMultiplier and persist isExact on guesses"
```

---

### Task 4: Functional test do `GET /rounds/:roundId/match` exposing `pointsMultiplier`

**Files:**
- Modify: `apps/api/tests/functional/matches.spec.ts`

- [ ] **Step 1: Adicionar test que verifica o campo no GET**

Em `apps/api/tests/functional/matches.spec.ts`, adicione:

```ts
test('GET /rounds/:roundId/match expõe pointsMultiplier', async ({ client, assert }) => {
  const round = await RoundFactory.with('season').create()
  await MatchFactory.merge({
    roundId: round.id,
    pointsMultiplier: 2,
  }).create()

  const res = await client.get(`/api/v1/rounds/${round.id}/match`).headers(HEADERS)
  res.assertStatus(200)
  assert.equal(res.body().pointsMultiplier, 2)
})
```

- [ ] **Step 2: Rodar — verde**

Run:
```bash
nvm use && cd apps/api && node ace test functional --files='tests/functional/matches.spec.ts'
```
Expected: **PASS** — `presentMatch` (Task 1) já expõe o campo, então passa direto.

- [ ] **Step 3: Commit**

```bash
git add apps/api/tests/functional/matches.spec.ts
git commit -m "test(api): GET match endpoint exposes pointsMultiplier"
```

---

### Self-review checklist

- [ ] `packages/shared/dist/` foi rebuildado (sem isso, `apps/api` lê o tipo antigo via symlink → `dist/index.js`)
- [ ] Todos os 3 retornos de match no `MatchesController` (show + upsert insert + upsert update) usam `presentMatch`
- [ ] Finalizer chama `calculatePoints` com 3 args; `update` persiste `{ points, isExact }`
- [ ] `previewFinalize` retorna `pointsMultiplier` no envelope (template precisa disso no plan 8.4)
- [ ] Agregação de `exactScoresCount` usa `g.isExact === true`, não `g.points === 3`
- [ ] Testes pré-existentes continuam passando (multiplier default 1 no `MatchFactory`)

### Próximo plano

**8.4** — Templates de WhatsApp (`round_opened`, `round_opened_dm`, `match_reminder`, `match_finished`) ganham `pointsMultiplier?` no input e renderizam header/disclaimer; `WhatsAppNotifier` repassa; jobs (`OpenRoundJob`, `MatchReminderJob`, `SyncScoresJob`) leem do match e passam adiante.
