# Plano 2.0 — Cleanup do pivot API-Football → football-data.org

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans.

**Goal:** Remover os artefatos do plano 2.1/2.3 legado (API-Football), deletar o seeder das 38 rodadas e migrar a coluna `seasons.external_league_id` (number) para `external_competition_code` (text), que passa a guardar o code do football-data.org (ex: `"BSA"`). Após este plano, `POST /seasons/:id/sync` volta ao stub pré-2.3.

**Architecture:** Só cleanup e schema. Mantém plano 2.2 (`featured_match_picker`) intocado.

**Pré-requisito:** Planos 1.x e 2.2 concluídos (6 testes do picker passando).

**Critério de aceitação:**
- Pastas `app/integrations/api_football/` e arquivos associados deletados.
- `database/seeders/rounds_seeder.ts` deletado.
- Migration 0007 troca `external_league_id` por `external_competition_code`.
- Planos obsoletos (2.1/2.3/2.4 com data 2026-04-23) removidos — **já feito pelo autor deste plano**.
- `pnpm typecheck` passa.
- `node ace test` → ~75 passando (68 plano 1 + 6 picker + 1 stub restaurado).

> ⚠️ **Commits manuais.**

---

## Task 1: Deletar arquivos da integração API-Football e o seeder

**Files:**
- Delete: `apps/api/app/integrations/api_football/` (dir inteiro)
- Delete: `apps/api/app/services/fixtures_sync_service.ts`
- Delete: `apps/api/tests/unit/api_football_mappers.spec.ts`
- Delete: `apps/api/tests/helpers/api_football_mock.ts`
- Delete: `apps/api/database/seeders/rounds_seeder.ts`

- [ ] **Step 1: Remover arquivos**

```bash
rm -r apps/api/app/integrations/api_football
rm apps/api/app/services/fixtures_sync_service.ts
rm apps/api/tests/unit/api_football_mappers.spec.ts
rm apps/api/tests/helpers/api_football_mock.ts
rm apps/api/database/seeders/rounds_seeder.ts
```

Se `apps/api/tests/helpers/` ficar vazio, remover o dir também.

---

## Task 2: Reverter SeasonsController.sync ao stub

**Files:**
- Modify: `apps/api/app/controllers/seasons_controller.ts`

- [ ] **Step 1: Remover injeção de `FixturesSyncService` e restaurar stub**

Remover `import FixturesSyncService from '#services/fixtures_sync_service'` e a linha `private fixturesSyncService: FixturesSyncService` do constructor. Método `sync`:

```ts
async sync({ params, response }: HttpContext) {
  // Stub: implementação real vem no plano 2.3 (football-data.org).
  return response.accepted({
    message: 'sync será implementado no plano 2 (football-data.org)',
    seasonId: params.id,
  })
}
```

---

## Task 3: Reverter `tests/functional/seasons.spec.ts`

**Files:**
- Modify: `apps/api/tests/functional/seasons.spec.ts`

- [ ] **Step 1: Remover imports de mock e os dois testes funcionais do sync**

Apagar os testes `POST /seasons/:id/sync cria matches...` e `POST /seasons/:id/sync é idempotente...`. Apagar imports: `app`, `ApiFootballClient`, `RoundFactory`, `FakeApiFootballClient`, `fakeFixture`, `fakeStandings`.

- [ ] **Step 2: Restaurar o teste do stub**

```ts
test('POST /seasons/:id/sync retorna 202 (stub)', async ({ client }) => {
  const season = await SeasonFactory.create()
  const res = await client.post(`/api/v1/seasons/${season.id}/sync`).headers(HEADERS)
  res.assertStatus(202)
})
```

---

## Task 4: Migration 0007 — `external_league_id` → `external_competition_code`

**Files:**
- Create: `apps/api/database/migrations/0007_rename_seasons_external_league_id_to_competition_code.ts`

- [ ] **Step 1: Criar migration**

```ts
import { BaseSchema } from '@adonisjs/lucid/schema'

export default class extends BaseSchema {
  protected tableName = 'seasons'

  async up() {
    this.schema.alterTable(this.tableName, (table) => {
      table.dropUnique(['year', 'external_league_id'])
      table.dropColumn('external_league_id')
    })
    this.schema.alterTable(this.tableName, (table) => {
      table.text('external_competition_code').notNullable().defaultTo('BSA')
      table.unique(['year', 'external_competition_code'])
    })
  }

  async down() {
    this.schema.alterTable(this.tableName, (table) => {
      table.dropUnique(['year', 'external_competition_code'])
      table.dropColumn('external_competition_code')
    })
    this.schema.alterTable(this.tableName, (table) => {
      table.integer('external_league_id').notNullable().defaultTo(71)
      table.unique(['year', 'external_league_id'])
    })
  }
}
```

- [ ] **Step 2: Rodar migration no DB dev e regenerar schema**

```bash
cd apps/api
node ace migration:run
```

Isso atualiza `database/schema.ts` automaticamente — a classe `SeasonSchema` passa a ter `externalCompetitionCode: string`. **Não editar schema.ts à mão**.

---

## Task 5: Cascade de renomeação nos consumidores

**Files:**
- Modify: `apps/api/database/factories/season_factory.ts`
- Modify: `apps/api/app/validators/season_validator.ts`
- Modify: `apps/api/tests/functional/seasons.spec.ts`

- [ ] **Step 1: `season_factory.ts`**

Trocar `externalLeagueId: 71` por `externalCompetitionCode: 'BSA'`.

- [ ] **Step 2: `season_validator.ts`**

Trocar `externalLeagueId: vine.number()` por `externalCompetitionCode: vine.string().minLength(2).maxLength(10)`.

- [ ] **Step 3: `tests/functional/seasons.spec.ts` — BASE_PAYLOAD**

Trocar `externalLeagueId: 71` por `externalCompetitionCode: 'BSA'`.

---

## Task 6: Verificar suite

- [ ] **Step 1: Typecheck + testes**

```bash
pnpm --filter @palpites/api typecheck
node ace test
```

Expected: typecheck limpo, ~75 tests passando (68 plano 1 + 6 picker + 1 stub restaurado).

---

## Resumo do Plano 2.0

Fim:
- Código livre de qualquer traço do API-Football.
- Seeder de rodadas removido (sync vai criar rodadas sob demanda no 2.3).
- `seasons.external_competition_code` (text) no lugar de `external_league_id` (number).
- `POST /seasons/:id/sync` volta ao stub 202.
- Base limpa pra 2.1.

**Decisões registradas:**
- Migração faz `drop + add` (não `alter type`) pra simplicidade — default `'BSA'` preserva a row existente em dev.
- Planos obsoletos foram deletados junto com o ato de escrever este plano — não ficam em Task pra evitar retrabalho.
