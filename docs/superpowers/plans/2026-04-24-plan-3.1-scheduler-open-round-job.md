# Plano 3.1 — Scheduler infra + OpenRoundJob

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans.

**Goal:** Colocar a infra de scheduler (node-cron) no projeto e implementar o primeiro job automático — `OpenRoundJob`, que roda a cada 30 min, chama `FixturesSyncService.syncCurrentMatchday` para cada season ativa e transiciona `round.status: pending → open` quando o round já tem match.

**Architecture:**
- `start/scheduler.ts` (preload) registra os cron jobs. Só carrega em `environment: ['web']` pra não rodar em testes.
- `OpenRoundJob` em `app/jobs/open_round_job.ts` com DI. Pure orchestrator: lista seasons ativas, chama sync per season, flipa status. Retorna `OpenRoundReport` — útil pra log + debug.
- Erros por season não derrubam o job inteiro; são agregados em `errors[]` no report.

**Pré-requisito:** Fase 2 completa (`FixturesSyncService.syncCurrentMatchday` existe e funciona).

**Critério de aceitação:**
- `node-cron` instalado + tipado.
- `app/jobs/open_round_job.ts` testado (unit da lógica de transição + funcional end-to-end com `FakeFootballDataClient`).
- `start/scheduler.ts` registrado em `adonisrc.ts → preloads` com filtro `environment: ['web']`.
- Suite completa passa.

> ⚠️ **Commits manuais.**

---

## Task 1: Instalar node-cron + tipos

**Files:**
- Modify: `apps/api/package.json`

- [ ] **Step 1: Install**

```bash
nvm use
pnpm --filter @palpites/api add node-cron
pnpm --filter @palpites/api add -D @types/node-cron
```

- [ ] **Step 2: Adicionar alias `#jobs/*`**

No `apps/api/package.json → imports`, junto dos outros aliases:

```json
"#jobs/*": "./app/jobs/*.js",
```

- [ ] **Step 3: Typecheck**

```bash
pnpm --filter @palpites/api typecheck
```

---

## Task 2: `SeasonRepository.listActive()`

**Files:**
- Modify: `apps/api/app/repositories/season_repository.ts`

- [ ] **Step 1: Adicionar método**

```ts
listActive() {
  return Season.query().where('is_active', true)
}
```

Soft-delete já é filtrado pelo scope global do model.

---

## Task 3: `OpenRoundJob` service (DI + lógica)

**Files:**
- Create: `apps/api/app/jobs/open_round_job.ts`

Reporta o que fez por season. Erros isolados per-season (não param o job).

- [ ] **Step 1: Criar job**

```ts
import { inject } from '@adonisjs/core'
import logger from '@adonisjs/core/services/logger'
import FixturesSyncService, { type SyncReport } from '#services/fixtures_sync_service'
import RoundRepository from '#repositories/round_repository'
import SeasonRepository from '#repositories/season_repository'

export interface OpenRoundRun {
  seasonId: string
  syncReport: SyncReport | null
  roundOpened: boolean
  error?: string
}

export interface OpenRoundReport {
  runs: OpenRoundRun[]
}

@inject()
export default class OpenRoundJob {
  constructor(
    private seasonRepository: SeasonRepository,
    private roundRepository: RoundRepository,
    private fixturesSyncService: FixturesSyncService
  ) {}

  async run(): Promise<OpenRoundReport> {
    const seasons = await this.seasonRepository.listActive()
    const runs: OpenRoundRun[] = []

    for (const season of seasons) {
      try {
        const syncReport = await this.fixturesSyncService.syncCurrentMatchday(season.id)
        const round = await this.roundRepository.findBySeasonAndNumber(
          season.id,
          syncReport.currentMatchday
        )

        let roundOpened = false
        if (round && round.status === 'pending' && syncReport.match) {
          await this.roundRepository.update(round, { status: 'open' } as any)
          roundOpened = true
        }

        runs.push({ seasonId: season.id, syncReport, roundOpened })
      } catch (err) {
        const msg = err instanceof Error ? err.message : String(err)
        logger.error({ seasonId: season.id, err: msg }, 'OpenRoundJob: falha em season')
        runs.push({
          seasonId: season.id,
          syncReport: null,
          roundOpened: false,
          error: msg,
        })
      }
    }

    logger.info({ runs }, 'OpenRoundJob: finished')
    return { runs }
  }
}
```

- [ ] **Step 2: Typecheck**

---

## Task 4: `start/scheduler.ts` + preload

**Files:**
- Create: `apps/api/start/scheduler.ts`
- Modify: `apps/api/adonisrc.ts`

- [ ] **Step 1: Criar `scheduler.ts`**

```ts
import cron from 'node-cron'
import app from '@adonisjs/core/services/app'
import logger from '@adonisjs/core/services/logger'
import OpenRoundJob from '#jobs/open_round_job'

/**
 * Registra os cron jobs ao subir a aplicação.
 * Esse arquivo é importado como preload via adonisrc.ts com environment=['web'],
 * pra não rodar em testes nem no REPL.
 */

const TZ = 'America/Sao_Paulo'

// OpenRoundJob: a cada 30 min
cron.schedule(
  '*/30 * * * *',
  async () => {
    try {
      const job = await app.container.make(OpenRoundJob)
      await job.run()
    } catch (err) {
      logger.error({ err }, 'scheduler: OpenRoundJob crashed')
    }
  },
  { timezone: TZ }
)

logger.info('scheduler: cron jobs registered')
```

- [ ] **Step 2: Adicionar preload em `adonisrc.ts`**

```ts
preloads: [
  () => import('#start/routes'),
  () => import('#start/kernel'),
  () => import('#start/validator'),
  {
    file: () => import('#start/scheduler'),
    environment: ['web'],
  },
],
```

`environment: ['web']` garante que o scheduler **não** carrega em `test`/`console`/`repl`. Isso é importante: sem isso, cada `node ace test` ficaria registrando crons.

- [ ] **Step 3: Sanity — iniciar dev e verificar log**

```bash
pnpm --filter @palpites/api dev
```

No stdout deve aparecer `scheduler: cron jobs registered` junto da boot da API. Ctrl+C depois.

---

## Task 5: Testes

**Files:**
- Create: `apps/api/tests/functional/open_round_job.spec.ts`

Unit testar a lógica de transição seria redundante (o trabalho pesado está em `syncCurrentMatchday` e `roundRepository.update`). Vamos direto pro funcional com mocks.

- [ ] **Step 1: Criar spec**

```ts
import { test } from '@japa/runner'
import app from '@adonisjs/core/services/app'
import testUtils from '@adonisjs/core/services/test_utils'
import FootballDataClient from '#integrations/football_data/client'
import OpenRoundJob from '#jobs/open_round_job'
import { SeasonFactory } from '#factories/season_factory'
import { RoundFactory } from '#factories/round_factory'
import { MatchFactory } from '#factories/match_factory'
import {
  FakeFootballDataClient,
  fakeStandings,
  fakeMatch,
} from '#tests/helpers/football_data_mock'

test.group('OpenRoundJob', (group) => {
  group.each.setup(() => testUtils.db().wrapInGlobalTransaction())

  test('abre round pendente após criar match via sync', async ({ assert }) => {
    const season = await SeasonFactory.merge({
      isActive: true,
      year: 2026,
      externalCompetitionCode: 'BSA',
    }).create()

    const fake = new FakeFootballDataClient()
    fake.standings = fakeStandings(12, 2026, { 1: 30, 2: 25 })
    fake.matchesByMatchday.set('2026:12', [fakeMatch(1001, 1, 2, 12)])

    app.container.swap(FootballDataClient, () => fake as any)
    try {
      const job = await app.container.make(OpenRoundJob)
      const report = await job.run()

      assert.lengthOf(report.runs, 1)
      const run = report.runs[0]
      assert.equal(run.seasonId, season.id)
      assert.isNotNull(run.syncReport)
      assert.equal(run.syncReport!.currentMatchday, 12)
      assert.equal(run.roundOpened, true)
    } finally {
      app.container.restore(FootballDataClient)
    }
  })

  test('é idempotente: segunda execução não reabre', async ({ assert }) => {
    const season = await SeasonFactory.merge({
      isActive: true,
      year: 2026,
      externalCompetitionCode: 'BSA',
    }).create()

    const fake = new FakeFootballDataClient()
    fake.standings = fakeStandings(12, 2026, { 1: 30, 2: 25 })
    fake.matchesByMatchday.set('2026:12', [fakeMatch(1001, 1, 2, 12)])

    app.container.swap(FootballDataClient, () => fake as any)
    try {
      const job = await app.container.make(OpenRoundJob)
      await job.run()
      const second = await job.run()

      assert.equal(second.runs[0].roundOpened, false)
    } finally {
      app.container.restore(FootballDataClient)
    }
  })

  test('ignora season inativa', async ({ assert }) => {
    await SeasonFactory.merge({ isActive: false }).create()

    const fake = new FakeFootballDataClient()
    app.container.swap(FootballDataClient, () => fake as any)
    try {
      const job = await app.container.make(OpenRoundJob)
      const report = await job.run()
      assert.lengthOf(report.runs, 0)
    } finally {
      app.container.restore(FootballDataClient)
    }
  })

  test('erro em uma season não quebra o job: registra em errors', async ({ assert }) => {
    await SeasonFactory.merge({ isActive: true, externalCompetitionCode: 'BSA' }).create()

    const fake = new FakeFootballDataClient()
    // standings vazio → fetchStandings throws no fake
    app.container.swap(FootballDataClient, () => fake as any)
    try {
      const job = await app.container.make(OpenRoundJob)
      const report = await job.run()

      assert.lengthOf(report.runs, 1)
      assert.isDefined(report.runs[0].error)
    } finally {
      app.container.restore(FootballDataClient)
    }
  })

  test('não sobrescreve round que já está open', async ({ assert }) => {
    const season = await SeasonFactory.merge({
      isActive: true,
      year: 2026,
      externalCompetitionCode: 'BSA',
    }).create()
    const round = await RoundFactory.merge({
      seasonId: season.id,
      number: 12,
      status: 'open',
    }).create()
    await MatchFactory.merge({ roundId: round.id }).create()

    const fake = new FakeFootballDataClient()
    fake.standings = fakeStandings(12, 2026, { 1: 30, 2: 25 })
    fake.matchesByMatchday.set('2026:12', [fakeMatch(1001, 1, 2, 12)])

    app.container.swap(FootballDataClient, () => fake as any)
    try {
      const job = await app.container.make(OpenRoundJob)
      const report = await job.run()

      assert.equal(report.runs[0].roundOpened, false)
      const fresh = await round.refresh()
      assert.equal(fresh.status, 'open')
    } finally {
      app.container.restore(FootballDataClient)
    }
  })
})
```

- [ ] **Step 2: Rodar suite**

```bash
node ace test
```

Expected: ~86 (Fase 2) + 5 novos = 91 tests.

---

## Resumo do Plano 3.1

- `node-cron` instalado + alias `#jobs/*`.
- `OpenRoundJob` via DI reutilizando `FixturesSyncService` (nenhuma duplicação).
- Scheduler preloaded só em `environment: ['web']` — testes ficam limpos.
- 5 testes funcionais cobrindo happy path, idempotência, season inativa, erro per-season, não-sobrescrita.

**Decisões:**
- Cron a cada 30 min (escolha do usuário; cabe folgado no free tier 10 req/min).
- TZ `America/Sao_Paulo` no `node-cron.schedule`.
- `logger` do Adonis pra observabilidade — job nunca lança, sempre retorna report.
- `pending` permanece como estado inicial real (ver CLAUDE.md root, seção "status").
