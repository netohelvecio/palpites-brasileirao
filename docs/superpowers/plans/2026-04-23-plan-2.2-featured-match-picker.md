# Plano 2.2 — Featured Match Picker (TDD)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development + superpowers:executing-plans.

**Goal:** Implementar função pura que escolhe o "jogo da rodada" — o confronto entre dois times cuja soma de pontos atual na tabela é a maior da rodada.

**Architecture:** Função pura em `app/services/featured_match_picker.ts`, sem dependência de banco, HTTP ou Lucid. Recebe lista de candidatos (fixtures) + tabela (standings) e devolve o escolhido ou erro.

**Tech Stack:** TypeScript, Japa.

**Spec de referência:** `docs/superpowers/specs/2026-04-20-palpites-brasileirao-design.md` (seção 4.2 — Abertura de rodada).

**Pré-requisito:** Plano 2.1 concluído (types da API-Football disponíveis).

**Critério de aceitação:** 6+ unit tests cobrindo happy path + empates + times faltando no standings + lista vazia.

> ⚠️ **Commits manuais.**

---

## Task 1: Escrever testes unitários (red)

**Files:**
- Create: `apps/api/tests/unit/featured_match_picker.spec.ts`

- [ ] **Step 1: Escrever testes**

Arquivo: `apps/api/tests/unit/featured_match_picker.spec.ts`
```ts
import { test } from '@japa/runner'
import {
  pickFeaturedMatch,
  type FixtureCandidate,
  type StandingEntry,
} from '#services/featured_match_picker'

const STANDINGS: StandingEntry[] = [
  { teamId: 1, points: 30 },  // Palmeiras
  { teamId: 2, points: 25 },  // Flamengo
  { teamId: 3, points: 20 },  // São Paulo
  { teamId: 4, points: 15 },  // Santos
  { teamId: 5, points: 10 },  // Corinthians
]

function fixture(id: number, homeId: number, awayId: number): FixtureCandidate {
  return {
    externalId: id,
    homeTeamId: homeId,
    homeTeamName: `Team${homeId}`,
    awayTeamId: awayId,
    awayTeamName: `Team${awayId}`,
    kickoffAt: new Date('2026-05-04T20:00:00Z'),
  }
}

test.group('FeaturedMatchPicker', () => {
  test('escolhe o confronto com maior soma de pontos dos dois times', ({ assert }) => {
    // Jogos: (1+2)=55, (3+4)=35, (5+1)=40
    const r = pickFeaturedMatch(
      [fixture(100, 3, 4), fixture(101, 1, 2), fixture(102, 5, 1)],
      STANDINGS
    )
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 101)
  })

  test('mantém o primeiro em caso de empate total', ({ assert }) => {
    // Dois jogos com soma 35: (3+4) e (2+3)
    const r = pickFeaturedMatch(
      [fixture(200, 3, 4), fixture(201, 2, 3)],
      [
        { teamId: 2, points: 15 },
        { teamId: 3, points: 20 },
        { teamId: 4, points: 15 },
      ]
    )
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 200)  // veio antes
  })

  test('times sem registro no standings contam como 0 pts', ({ assert }) => {
    // Jogo A: (1+2)=55; Jogo B: (999+888)=0
    const r = pickFeaturedMatch(
      [fixture(300, 999, 888), fixture(301, 1, 2)],
      STANDINGS
    )
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 301)
  })

  test('retorna erro quando lista de fixtures é vazia', ({ assert }) => {
    const r = pickFeaturedMatch([], STANDINGS)
    assert.equal(r.ok, false)
  })

  test('funciona com 1 fixture apenas', ({ assert }) => {
    const r = pickFeaturedMatch([fixture(400, 1, 5)], STANDINGS)
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 400)
  })

  test('standings vazio: todos contam 0, primeiro vence', ({ assert }) => {
    const r = pickFeaturedMatch([fixture(500, 1, 2), fixture(501, 3, 4)], [])
    assert.equal(r.ok, true)
    if (r.ok) assert.equal(r.match.externalId, 500)
  })
})
```

- [ ] **Step 2: Rodar — devem FALHAR (implementação não existe)**

```bash
cd apps/api
node ace test unit --files='tests/unit/featured_match_picker.spec.ts'
```

Expected: 6 tests failing com "Cannot find module".

---

## Task 2: Implementar o picker (green)

**Files:**
- Create: `apps/api/app/services/featured_match_picker.ts`

- [ ] **Step 1: Escrever implementação**

Arquivo: `apps/api/app/services/featured_match_picker.ts`
```ts
export interface FixtureCandidate {
  externalId: number
  homeTeamId: number
  homeTeamName: string
  awayTeamId: number
  awayTeamName: string
  kickoffAt: Date
}

export interface StandingEntry {
  teamId: number
  points: number
}

export type PickResult =
  | { ok: true; match: FixtureCandidate }
  | { ok: false; reason: string }

export function pickFeaturedMatch(
  fixtures: FixtureCandidate[],
  standings: StandingEntry[]
): PickResult {
  if (fixtures.length === 0) {
    return { ok: false, reason: 'nenhum jogo disponível para a rodada' }
  }

  const pointsByTeam = new Map(standings.map((s) => [s.teamId, s.points]))
  const pointsOf = (teamId: number) => pointsByTeam.get(teamId) ?? 0

  let best: { match: FixtureCandidate; sum: number } | null = null

  for (const fixture of fixtures) {
    const sum = pointsOf(fixture.homeTeamId) + pointsOf(fixture.awayTeamId)
    if (!best || sum > best.sum) {
      best = { match: fixture, sum }
    }
  }

  return { ok: true, match: best!.match }
}
```

- [ ] **Step 2: Rodar — devem PASSAR**

```bash
node ace test unit --files='tests/unit/featured_match_picker.spec.ts'
```

Expected: 6 passing.

- [ ] **Step 3: Rodar suite inteira**

```bash
node ace test
```

Expected: tudo passa (68 anteriores + 6 novos = 74).

---

## Resumo do Plano 2.2

Ao final:
- Função pura `pickFeaturedMatch(fixtures, standings)` disponível.
- 6 unit tests cobrindo happy path + empates + times faltando + inputs vazios.
- Sem dependência de Lucid, HTTP ou DI — simples e reusável.

**Decisões:**
- Stable sort (primeiro a achar o máximo vence) → empate resolvido pela ordem da lista de entrada.
- Time sem standings conta como 0 pontos (jogadores novos na Série A quando sobem, por exemplo).
- Retorna discriminated union `PickResult` pro caller tratar erro sem throw.
