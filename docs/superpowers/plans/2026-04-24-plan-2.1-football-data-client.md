# Plano 2.1 — Football-Data.org HTTP Client

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans.

**Goal:** Client tipado (axios) para football-data.org v4, registrável via DI e mockável em testes. Substitui o `ApiFootballClient` removido no plano 2.0.

**Architecture:** Classe `FootballDataClient` em `app/integrations/football_data/client.ts` com 3 métodos:
- `fetchStandings(competitionCode)` — tabela + `currentMatchday` + season info
- `fetchMatchesByMatchday(competitionCode, season, matchday)` — lista de jogos de uma rodada
- `fetchMatchById(matchId)` — detalhes (placar, status) de um jogo

Auth via header `X-Auth-Token`. Types do payload em `types.ts`. `@inject()` pra DI.

**Tech Stack:** axios (já instalado), Adonis 7, TypeScript.

**Spec de referência:** `docs/superpowers/specs/2026-04-20-palpites-brasileirao-design.md` (Integrações Externas) — **verificar se spec foi atualizada após o pivot; se não, atualizar primeiro**.

**Pré-requisito:** Plano 2.0 concluído.

**Critério de aceitação:**
- `pnpm typecheck` passa.
- Classe instanciável via `new FootballDataClient()` e mockável via `app.container.swap(...)`.

> ⚠️ **Commits manuais.**

---

## Task 1: Env vars football-data

**Files:**
- Modify: `apps/api/start/env.ts`
- Modify: `apps/api/.env.example`, `.env`, `.env.test`

- [ ] **Step 1: Trocar env vars em `start/env.ts`**

Remover:
```ts
// API-Football
API_FOOTBALL_BASE_URL: ...,
API_FOOTBALL_KEY: ...,
```

Adicionar:
```ts
// Football-Data.org
FOOTBALL_DATA_BASE_URL: Env.schema.string({ format: 'url', tld: false }),
FOOTBALL_DATA_TOKEN: Env.schema.string.optional(),
```

Token opcional pelo mesmo motivo de antes (dev sem chave real, tests via mock).

- [ ] **Step 2: Atualizar `.env.example`, `.env`, `.env.test`**

Remover todas as linhas `API_FOOTBALL_*`. Adicionar (em cada um conforme apropriado):

`.env.example`:
```
# Football-Data.org
FOOTBALL_DATA_BASE_URL=https://api.football-data.org/v4
FOOTBALL_DATA_TOKEN=
```

`.env` (preencher token quando tiver conta real):
```
FOOTBALL_DATA_BASE_URL=https://api.football-data.org/v4
FOOTBALL_DATA_TOKEN=
```

`.env.test`:
```
FOOTBALL_DATA_BASE_URL=https://api.football-data.org/v4
FOOTBALL_DATA_TOKEN=test-token-unused
```

- [ ] **Step 3: Typecheck**

```bash
pnpm --filter @palpites/api typecheck
```

---

## Task 2: Types dos payloads

**Files:**
- Create: `apps/api/app/integrations/football_data/types.ts`

Subconjunto do payload — só o que consumimos.

- [ ] **Step 1: Criar `types.ts`**

```ts
/**
 * Subconjunto do payload da football-data.org v4 que consumimos.
 * Docs: https://docs.football-data.org/general/v4/index.html
 */

export type FootballDataMatchStatus =
  | 'SCHEDULED'
  | 'TIMED'
  | 'IN_PLAY'
  | 'PAUSED'
  | 'FINISHED'
  | 'SUSPENDED'
  | 'POSTPONED'
  | 'CANCELLED'
  | 'AWARDED'

export interface FootballDataTeam {
  id: number
  name: string
}

export interface FootballDataSeason {
  id: number
  startDate: string
  endDate: string
  currentMatchday: number
  winner: FootballDataTeam | null
}

export interface FootballDataStandingEntry {
  position: number
  team: FootballDataTeam
  points: number
  playedGames: number
}

export interface FootballDataStandingsResponse {
  competition: { id: number; name: string; code: string }
  season: FootballDataSeason
  standings: Array<{
    stage: string
    type: 'TOTAL' | 'HOME' | 'AWAY'
    group: string | null
    table: FootballDataStandingEntry[]
  }>
}

export interface FootballDataMatch {
  id: number
  utcDate: string
  status: FootballDataMatchStatus
  matchday: number
  homeTeam: FootballDataTeam
  awayTeam: FootballDataTeam
  score: {
    fullTime: { home: number | null; away: number | null }
    halfTime?: { home: number | null; away: number | null }
    winner: 'HOME_TEAM' | 'AWAY_TEAM' | 'DRAW' | null
  }
}

export interface FootballDataMatchesResponse {
  matches: FootballDataMatch[]
}
```

- [ ] **Step 2: Typecheck**

---

## Task 3: `FootballDataClient`

**Files:**
- Create: `apps/api/app/integrations/football_data/client.ts`

- [ ] **Step 1: Criar `client.ts`**

```ts
import axios, { type AxiosInstance } from 'axios'
import env from '#start/env'
import type {
  FootballDataMatch,
  FootballDataMatchesResponse,
  FootballDataStandingsResponse,
} from './types.js'

export class FootballDataError extends Error {
  constructor(
    public status: number,
    public body: unknown,
    message: string
  ) {
    super(message)
    this.name = 'FootballDataError'
  }
}

export default class FootballDataClient {
  private http: AxiosInstance

  constructor() {
    this.http = axios.create({
      baseURL: env.get('FOOTBALL_DATA_BASE_URL'),
      timeout: 15_000,
      headers: {
        'X-Auth-Token': env.get('FOOTBALL_DATA_TOKEN') ?? '',
      },
    })
  }

  async fetchStandings(competitionCode: string): Promise<FootballDataStandingsResponse> {
    return this.get<FootballDataStandingsResponse>(
      `/competitions/${competitionCode}/standings`
    )
  }

  async fetchMatchesByMatchday(
    competitionCode: string,
    season: number,
    matchday: number
  ): Promise<FootballDataMatch[]> {
    const res = await this.get<FootballDataMatchesResponse>(
      `/competitions/${competitionCode}/matches`,
      { season, matchday }
    )
    return res.matches ?? []
  }

  async fetchMatchById(matchId: number): Promise<FootballDataMatch | null> {
    try {
      return await this.get<FootballDataMatch>(`/matches/${matchId}`)
    } catch (err) {
      if (err instanceof FootballDataError && err.status === 404) return null
      throw err
    }
  }

  private async get<T>(path: string, params?: Record<string, unknown>): Promise<T> {
    try {
      const res = await this.http.get<T>(path, { params })
      return res.data
    } catch (err) {
      if (axios.isAxiosError(err)) {
        throw new FootballDataError(
          err.response?.status ?? 0,
          err.response?.data,
          `football-data ${path} failed: ${err.message}`
        )
      }
      throw err
    }
  }
}
```

- [ ] **Step 2: Typecheck**

- [ ] **Step 3: Sanity REPL**

```bash
cd apps/api
printf '(async () => { const m = await import("#integrations/football_data/client"); const c = new m.default(); console.log("ok:", typeof c.fetchStandings === "function" && typeof c.fetchMatchesByMatchday === "function" && typeof c.fetchMatchById === "function") })()\n' | node ace repl
```

Expected: `ok: true`.

---

## Resumo do Plano 2.1

- axios mantido, alias `#integrations/*` mantido (configurados no 2.1 antigo, não precisam mudar).
- Env vars `FOOTBALL_DATA_BASE_URL` e `FOOTBALL_DATA_TOKEN`.
- Types + `FootballDataClient` em `app/integrations/football_data/`.
- 0 testes unitários novos (thin wrapper axios; cobertura vem por integração nos planos 2.3 e 2.4 com mock).

**Decisões registradas:**
- Header `X-Auth-Token` (convenção da football-data.org).
- Token opcional no env → permite dev rodar sem chave quando não chama sync.
- `fetchMatchById` retorna `null` em 404 pra caller tratar "match deletado externamente" sem try/catch.
- Erros não-axios (rede, timeout) re-lançados como `FootballDataError(status=0)`.
- Timeout de 15s pra evitar requests pendurados.
