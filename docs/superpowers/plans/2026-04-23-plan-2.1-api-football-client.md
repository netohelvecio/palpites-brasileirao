# Plano 2.1 — API-Football HTTP Client

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development ou superpowers:executing-plans.

**Goal:** Criar um HTTP client tipado (axios) para a API-Football, com env vars e error handling, registrável no DI container e mockável em testes.

**Architecture:** Classe `ApiFootballClient` em `app/integrations/api_football/client.ts` com métodos tipados para os 3 endpoints que vamos usar (`fetchFixturesByRound`, `fetchStandings`, `fetchFixtureById`). Tipos do payload da API em `types.ts`. `@inject()` pra injetar em services que precisarem.

**Tech Stack:** axios, Adonis 7 (DI container), TypeScript.

**Spec de referência:** `docs/superpowers/specs/2026-04-20-palpites-brasileirao-design.md` (seção Integrações Externas).

**Pré-requisito:** Plano 1.8 concluído.

**Critério de aceitação:**
- `pnpm typecheck` passa
- Classe registrável via `app.container.make(ApiFootballClient)` e mockável via `app.container.swap(...)`.

> ⚠️ **Commits manuais.**

---

## Task 1: Instalar axios e adicionar env vars

**Files:**
- Modify: `apps/api/package.json` (dep)
- Modify: `apps/api/start/env.ts`
- Modify: `apps/api/.env.example`, `.env`, `.env.test`
- Modify: `apps/api/package.json` (`#integrations/*` alias)

- [ ] **Step 1: Instalar axios**

```bash
nvm use
pnpm --filter @palpites/api add axios
```

- [ ] **Step 2: Adicionar alias `#integrations/*` em `apps/api/package.json`**

```json
"#integrations/*": "./app/integrations/*.js",
```

(Coloca junto dos outros aliases no bloco `imports`.)

- [ ] **Step 3: Declarar env vars em `apps/api/start/env.ts`**

```ts
  // API-Football
  API_FOOTBALL_BASE_URL: Env.schema.string({ format: 'url', tld: false }),
  API_FOOTBALL_KEY: Env.schema.string.optional(),
```

`API_FOOTBALL_KEY` é opcional porque:
- Em testes usamos mock (não precisa de chave real)
- Em dev inicial também pode rodar sem, se ninguém chamar o sync

- [ ] **Step 4: Adicionar defaults em `.env.example`, `.env`, `.env.test`**

No `.env.example`:
```
# API-Football
API_FOOTBALL_BASE_URL=https://v3.football.api-sports.io
API_FOOTBALL_KEY=
```

No `.env` (copia do example, chave em branco até ter). No `.env.test`:
```
API_FOOTBALL_BASE_URL=https://v3.football.api-sports.io
API_FOOTBALL_KEY=test-key-unused
```

- [ ] **Step 5: Verificar typecheck**

```bash
pnpm --filter @palpites/api typecheck
```

Expected: sem erros.

---

## Task 2: Tipos de payload da API-Football

**Files:**
- Create: `apps/api/app/integrations/api_football/types.ts`

Tipos mínimos que vamos consumir dos responses. Não precisa mapear todos os campos — só os que usamos.

- [ ] **Step 1: Criar types.ts**

Arquivo: `apps/api/app/integrations/api_football/types.ts`
```ts
/**
 * Subconjunto do payload da API-Football que consumimos.
 * Docs: https://www.api-football.com/documentation-v3
 */

export interface ApiFootballEnvelope<T> {
  errors?: unknown
  results: number
  response: T[]
}

export interface ApiFootballFixture {
  fixture: {
    id: number
    date: string
    status: {
      short: ApiFootballFixtureStatus
      long: string
    }
  }
  league: {
    id: number
    season: number
    round: string
  }
  teams: {
    home: { id: number; name: string }
    away: { id: number; name: string }
  }
  goals: {
    home: number | null
    away: number | null
  }
}

export type ApiFootballFixtureStatus =
  // Scheduled-ish
  | 'TBD' | 'NS' | 'PST' | 'CANC' | 'ABD' | 'AWD' | 'WO'
  // Live-ish
  | '1H' | 'HT' | '2H' | 'ET' | 'BT' | 'P' | 'SUSP' | 'INT' | 'LIVE'
  // Finished
  | 'FT' | 'AET' | 'PEN'

export interface ApiFootballStandingEntry {
  rank: number
  team: { id: number; name: string }
  points: number
}

export interface ApiFootballStandingsResponse {
  league: {
    id: number
    season: number
    standings: ApiFootballStandingEntry[][]  // array de grupos (Brasileirão tem 1 grupo)
  }
}
```

- [ ] **Step 2: Typecheck**

```bash
pnpm --filter @palpites/api typecheck
```

---

## Task 3: ApiFootballClient class

**Files:**
- Create: `apps/api/app/integrations/api_football/client.ts`

Client singleton com 3 métodos:
- `fetchFixturesByRound(leagueId, season, roundLabel)` → lista de fixtures
- `fetchFixtureById(fixtureId)` → uma fixture
- `fetchStandings(leagueId, season)` → lista de standings

Erros: se HTTP >= 400, joga `ApiFootballError` com status e body.

- [ ] **Step 1: Criar client.ts**

Arquivo: `apps/api/app/integrations/api_football/client.ts`
```ts
import axios, { type AxiosInstance } from 'axios'
import env from '#start/env'
import type {
  ApiFootballEnvelope,
  ApiFootballFixture,
  ApiFootballStandingsResponse,
} from './types.js'

export class ApiFootballError extends Error {
  constructor(
    public status: number,
    public body: unknown,
    message: string
  ) {
    super(message)
    this.name = 'ApiFootballError'
  }
}

export default class ApiFootballClient {
  private http: AxiosInstance

  constructor() {
    this.http = axios.create({
      baseURL: env.get('API_FOOTBALL_BASE_URL'),
      timeout: 15_000,
      headers: {
        'x-apisports-key': env.get('API_FOOTBALL_KEY') ?? '',
      },
    })
  }

  async fetchFixturesByRound(
    leagueId: number,
    season: number,
    roundLabel: string
  ): Promise<ApiFootballFixture[]> {
    return this.get<ApiFootballFixture>('/fixtures', {
      league: leagueId,
      season,
      round: roundLabel,
    })
  }

  async fetchFixtureById(fixtureId: number): Promise<ApiFootballFixture | null> {
    const rows = await this.get<ApiFootballFixture>('/fixtures', { id: fixtureId })
    return rows[0] ?? null
  }

  async fetchStandings(leagueId: number, season: number): Promise<ApiFootballStandingsResponse[]> {
    return this.get<ApiFootballStandingsResponse>('/standings', {
      league: leagueId,
      season,
    })
  }

  private async get<T>(path: string, params: Record<string, unknown>): Promise<T[]> {
    try {
      const res = await this.http.get<ApiFootballEnvelope<T>>(path, { params })
      return res.data.response ?? []
    } catch (err) {
      if (axios.isAxiosError(err)) {
        throw new ApiFootballError(
          err.response?.status ?? 0,
          err.response?.data,
          `API-Football ${path} failed: ${err.message}`
        )
      }
      throw err
    }
  }
}
```

- [ ] **Step 2: Typecheck**

```bash
pnpm --filter @palpites/api typecheck
```

- [ ] **Step 3: Sanity: importar no REPL Adonis**

```bash
cd apps/api
node ace repl
```

Dentro do REPL:
```js
const { default: Client } = await import('#integrations/api_football/client')
const c = new Client()
console.log(typeof c.fetchFixturesByRound)  // 'function'
```

Não precisa chamar o endpoint real. Só confirma que a classe instancia sem erro.

`.exit` para sair.

---

## Resumo do Plano 2.1

Ao final:
- axios instalado
- Env vars `API_FOOTBALL_BASE_URL` e `API_FOOTBALL_KEY` declaradas e defaultadas
- Alias `#integrations/*` adicionado
- Types da API em `app/integrations/api_football/types.ts`
- Client class em `app/integrations/api_football/client.ts`
- 0 testes unitários novos (o client é thin wrapper em axios — cobertura vem por integração nos planos 2.3 e 2.4, com mock)

**Decisões registradas:**
- `API_FOOTBALL_KEY` opcional pra não quebrar dev quando ninguém chama sync.
- `timeout: 15s` pra evitar requests pendurados.
- Header `x-apisports-key` (formato direto da API) em vez de Bearer.
- Erros não-axios (rede, timeout) são re-lançados como `ApiFootballError` com `status: 0`.
